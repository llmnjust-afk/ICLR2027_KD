\section{Method}
\label{sec:method}

\ags{} enhances the training-free dataset distillation pipeline of MGD$^3$~\cite{chansantiago2025modeguided} by replacing its globally fixed classifier-free guidance with per-class adaptive guidance. The core module, \cags{} (Section~\ref{sec:cags}), assigns a per-class guidance strength based on a complexity metric. We additionally explored two complementary modules---\tags{} and \iast{} (Section~\ref{sec:tags-iast})---which address the temporal and IPC-budget axes respectively; both are investigated but ultimately not recommended based on negative ablation results. An overview of the framework is in \Cref{fig:framework}, and the full sampling algorithm is in Appendix~\ref{sec:algorithm}.

\begin{figure}[t]
    \centering
    \begin{tikzpicture}[
        node distance=0.35cm and 0.5cm,
        box/.style={draw, rounded corners=2pt, minimum width=2.2cm, minimum height=0.6cm, align=center, font=\footnotesize, fill=blue!8},
        databox/.style={draw, rounded corners=2pt, minimum width=1.8cm, minimum height=0.6cm, align=center, font=\footnotesize, fill=gray!10},
        resultbox/.style={draw, rounded corners=2pt, minimum width=2.0cm, minimum height=0.6cm, align=center, font=\footnotesize, fill=green!10},
        arrow/.style={-Stealth, thick, draw=gray!70},
    ]
   
    \node[databox] (real) {Real\\Training Images};
    \node[box, right=of real] (vae) {VAE Feature\\Extraction};
    \node[box, right=of vae] (kmeans) {$K$-means\\Clustering};
    \node[box, right=of kmeans] (score) {Complexity\\Score $\rho_c$};

   
    \node[box, below=0.5cm of $(vae)!0.5!(kmeans)$, minimum width=4.5cm] (cags) {\cags{}: $\lambda_c = 0.06\,\sigma\!\bigl(s(0.5\hat{H}_c + 0.5\hat{v}_c) - c_0\bigr)$};

   
    \node[box, below=of cags, minimum width=2.4cm, fill=blue!15] (dit) {Frozen DiT\\(per-class CFG)};
    \node[resultbox, right=of dit] (synth) {Synthetic\\Dataset};

   
    \draw[arrow] (real) -- (vae);
    \draw[arrow] (vae) -- (kmeans);
    \draw[arrow] (kmeans) -- (score);
    \draw[arrow] (score.south) |- (cags.east);
    \draw[arrow] (cags) -- (dit);
    \draw[arrow] (dit) -- (synth);

   
    \node[font=\scriptsize\itshape, gray, above=0.05cm of vae] {One-time analysis (cached)};
    \end{tikzpicture}
    \caption{Overview of the \ags{} framework. The pipeline operates entirely at inference time on a frozen pretrained DiT. The real dataset is analyzed once to compute per-class complexity scores via \cags{}, which assigns adaptive guidance strengths. No gradient computation or model training is required.}
    \label{fig:framework}
\end{figure}

\subsection{Background: Diffusion Models and Classifier-Free Guidance}
\label{sec:background}

Denoising diffusion probabilistic models~\cite{ho2020denoising} learn to reverse a gradual noising process. A neural network $\epsilon_\theta(\mathbf{x}_t, t, c)$ is trained to predict the noise at each timestep, conditioned on a class label $c$. Classifier-free guidance (CFG)~\cite{ho2022classifierfree} enhances conditional generation by interpolating between conditional and unconditional score predictions:
\begin{equation}
    \tilde{\epsilon}_\theta(\mathbf{x}_t, t, c) = (1 + \lambda)\,\epsilon_\theta(\mathbf{x}_t, t, c) - \lambda\,\epsilon_\theta(\mathbf{x}_t, t, \varnothing),
    \label{eq:cfg}
\end{equation}
where $\lambda \geq 0$ is the guidance scale and $\varnothing$ denotes the null class. The scale $\lambda$ controls the quality--diversity trade-off: higher values produce samples that adhere more faithfully to the conditioning but sacrifice diversity. In MGD$^3$~\cite{chansantiago2025modeguided}, the pretrained DiT-XL/2~\cite{peebles2022scalable} is frozen, and synthetic images are generated using a fixed $\lambda$ for all classes and timesteps, starting from a high-noise timestep $t_{\text{start}}$.

\subsection{Class-Adaptive Guidance Strength (CAGS)}
\label{sec:cags}

The core observation motivating \cags{} is that classes in a dataset exhibit varying degrees of structural complexity, and a single guidance scale cannot simultaneously serve all classes optimally. A class with high intra-class variance (e.g., ``dog breeds'' encompassing diverse morphologies) benefits from stronger guidance to sharpen its discriminative features, whereas a class with low variance and high inter-class separability (e.g., ``parachute'' vs.\ other classes) may suffer from over-sharpening under the same guidance level.

\paragraph{Multi-factor complexity metric.}
We quantify the complexity of each class $c$ through four complementary factors, computed from real training images via VAE feature extraction and $K$-means clustering (with silhouette-based $K \in [2, 20]$): \textbf{mode count} ($\kappa_c$, number of visual modes), \textbf{cluster entropy} ($H_c$, balance of cluster sizes), \textbf{intra-class variance} ($v_c$, feature dispersion), and \textbf{inter-class separability} ($d_c$, distance to nearest class centroid normalized by intra-class spread). Through systematic factor ablation (\Cref{sec:factor-ablation}) and weight grid search (Appendix~\ref{sec:weight-learning}), we find that only entropy and intra-class variance are effective: mode count is nearly constant ($K{=}2$ for 94\% of classes) and separability produces near-constant guidance (std $0.006$). The optimal configuration uses equal weights $(\alpha, \beta, \gamma, \delta) = (0, 0.5, 0.5, 0)$, yielding the simplified complexity score:
\begin{equation}
    \rho_c = \sigma\!\left(s \cdot \left(0.5\,\hat{H}_c + 0.5\,\hat{v}_c\right) - c_0\right),
    \label{eq:complexity-simplified}
\end{equation}
where $\hat{H}_c$ and $\hat{v}_c$ are min-max normalized, $\sigma(\cdot)$ is the sigmoid, $s = 3.0$ is the slope, and $c_0 = 0.6$ is the center. The score maps to a per-class guidance scale $\lambda_c = \lambda_{\min} + (\lambda_{\max} - \lambda_{\min}) \cdot \rho_c$, where $[\lambda_{\min}, \lambda_{\max}] = [0.0, 0.06]$. The sigmoid provides a bounded, monotonic approximation of the theoretically predicted linear scaling $\lambda_c^* \propto v_c / \sigma_c^2$ (Proposition~\ref{prop:opt-lambda}), mapping unbounded complexity scores to the valid guidance range; see Appendix~\ref{sec:theory-impl} for the full theory-to-implementation discussion. Classes with higher complexity receive stronger guidance. The general four-factor formulation is $\rho_c = \sigma\!\left(s \cdot (\alpha\hat{\kappa}_c + \beta\hat{H}_c + \gamma\hat{v}_c - \delta\hat{d}_c) - c_0\right)$; separability enters negatively since higher separability implies lower complexity.

\paragraph{Theoretical motivation.}
Section~\ref{sec:theory} proves that $\lambda_c^* \propto v_c / \sigma_c^2$ is increasing in both $v_c$ and $H_c$ (Proposition~\ref{prop:opt-lambda}), and the adaptivity gap grows with cross-class heterogeneity (Theorem~\ref{thm:adaptivity-gap}).

\subsection{Additional Explorations: TAGS and IAST}
\label{sec:tags-iast}

We explored two complementary modules that address secondary axes of variation. Both are \emph{not recommended} based on negative ablation results (Section~\ref{sec:module-ablation}). \tags{} (Timestep-Adaptive Guidance Schedule) replaces the constant $\lambda$ with a time-varying schedule $\lambda(t)$, concentrating guidance at high-noise timesteps. None of the three evaluated schedules improves over \cags{}-alone, because the optimal $\lambda_c^*$ depends on class properties, not the noise level. \iast{} (IPC-Adaptive Guidance Range) scales the guidance range by a logarithmic factor of IPC; at IPC=1 ($s \approx 0.29$), it eliminates 92\% of the per-class differentiation, hurting rather than helping.

\subsection{\ags{} Sampler}
\label{sec:unified}

The \cags{} module composes into a single sampling procedure (Algorithm~\ref{alg:ags}, Appendix). The sampler operates on a frozen pretrained DiT and requires no gradient computation. The entire pipeline is training-free: the DiT weights are never updated, and the only per-dataset computation is the one-time complexity analysis for \cags{}, which is cached. This makes \ags{} as simple to deploy as \mgd{} while substantially improving the quality of the distilled dataset through principled per-class guidance adaptation.
