\subsection{Comparison with Published DD Methods}
\label{sec:dd-baselines}

To contextualize \cags{} within the broader dataset distillation landscape, \Cref{tab:dd-baselines} compares our results on ImageNet-100 (100-class, IPC=10) against published numbers from several DD methods, as reported in the MGD$^3$ paper~\cite{chansantiago2025modeguided}. We include Random selection, Herding, IDC~\cite{kim2022dataset}, MinMaxDiff~\cite{fan2025generative}, and MGD$^3$ itself. Additionally, we implement D4M~\cite{su2024dataset} under our exact protocol: D4M encodes real images through the VAE, adds partial noise at an intermediate timestep ($t_{\text{start}}$ out of 50 denoising steps), and denoises using the same frozen DiT-XL/2 generator with the same CFG scale ($\lambda_{\text{CFG}} = 4.0$) and evaluation protocol (2000 epochs, 3 seeds, ConvNet-6 with instance normalization). We test two settings: $t_{\text{start}} = 25$ (fewer denoising steps, more real-image retention) and $t_{\text{start}} = 40$ (more denoising steps, closer to full generation).

\begin{table}[t]
\centering
\caption{Comparison with DD methods on ImageNet-100 (100-class, IPC=10). Published baselines (Random through MGD$^3$) are drawn from the MGD$^3$ paper~\cite{chansantiago2025modeguided}. D4M is our implementation under the same protocol. Our results (Unguided, \cags{}) use the same evaluation protocol (best-accuracy tracking, instance normalization, 3 seeds). \cags{} surpasses all published baselines on all three architectures, including MGD$^3$ itself, while D4M's performance is highly sensitive to the denoising start timestep $t_{\text{start}}$.}
\label{tab:dd-baselines}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{ConvNet-6} & \textbf{ResNetAP-10} & \textbf{ResNet-18} \\
\midrule
\multicolumn{4}{l}{\emph{Published baselines (from MGD$^3$ paper)}} \\
Random & $17.0 \pm 0.3$ & $19.1 \pm 0.4$ & $17.5 \pm 0.5$ \\
Herding & $17.2 \pm 0.3$ & $19.8 \pm 0.3$ & $16.1 \pm 0.2$ \\
IDC-1~\cite{kim2022dataset} & $24.3 \pm 0.5$ & $25.7 \pm 0.1$ & $25.1 \pm 0.2$ \\
MinMaxDiff~\cite{fan2025generative} & $22.3 \pm 0.5$ & $24.8 \pm 0.2$ & $22.5 \pm 0.3$ \\
MGD$^3$~\cite{chansantiago2025modeguided} & $23.4 \pm 0.9$ & $25.8 \pm 0.5$ & $23.6 \pm 0.4$ \\
\midrule
\multicolumn{4}{l}{\emph{Fair comparison (same DiT generator, same eval protocol)}} \\
D4M~\cite{su2024dataset} ($t{=}25$) & $20.21 \pm 0.39$ & --- & --- \\
D4M~\cite{su2024dataset} ($t{=}40$) & $23.07 \pm 0.59$ & --- & --- \\
Unguided & $22.79 \pm 0.18$ & $26.65 \pm 0.16$ & $26.49 \pm 0.42$ \\
\cags{} (4-factor) $[0.0, 0.06]$ & $25.29 \pm 0.27$ & $\mathbf{30.70 \pm 0.07}$ & $\mathbf{31.14 \pm 0.34}$ \\
\cags{} (2-factor, optimal) & $\mathbf{25.70 \pm 0.07}$ & $29.52 \pm 0.08$ & $30.50 \pm 0.07$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{\cags{} surpasses all published baselines.}
On ConvNet-6, the optimal two-factor \cags{} achieves $25.70\%$, surpassing MGD$^3$'s published $23.4\%$ by $+2.30\%$ absolute and IDC-1's $24.3\%$ by $+1.40\%$. On ResNet-18, the four-factor \cags{} achieves $31.14\%$ and the two-factor optimal achieves $30.50\%$, surpassing MGD$^3$'s published $23.6\%$ by $+7.54\%$ and $+6.90\%$ absolute respectively, and IDC-1's $25.1\%$ by $+6.04\%$ and $+5.40\%$. On ResNetAP-10, the four-factor \cags{} achieves $30.70\%$ and the two-factor optimal achieves $29.52\%$, exceeding MGD$^3$'s $25.8\%$ by $+4.90\%$ and $+3.72\%$ and IDC-1's $25.7\%$ by $+5.00\%$ and $+3.82\%$. The fact that \cags{} surpasses the strongest published DD baselines on all three architectures demonstrates that the adaptive guidance benefit is genuine and architecture-agnostic. Notably, the four-factor \cags{} outperforms the two-factor optimal on both ResNet-18 ($31.14\%$ vs.\ $30.50\%$) and ResNetAP-10 ($30.70\%$ vs.\ $29.52\%$), while the two-factor optimal wins on ConvNet-6 ($25.70\%$ vs.\ $25.29\%$), suggesting that the additional complexity factors provide complementary signal on higher-capacity architectures.

\paragraph{D4M is sensitive to the denoising start timestep.}
To comprehensively characterize D4M's sensitivity to the noise injection level, we sweep $t_{\text{start}} \in \{5, 10, 15, 20, 25, 30, 35, 40, 45\}$ under the same DiT generator and evaluation protocol (\Cref{tab:d4m-sensitivity}). The results reveal a monotonic increasing trend: D4M accuracy rises steadily from $15.06\%$ at $t_{\text{start}}{=}5$ to $23.07\%$ at $t_{\text{start}}{=}40$, then plateaus. At low $t_{\text{start}}$ values, D4M substantially \emph{underperforms} unguided generation---at $t_{\text{start}}{=}5$, D4M achieves only $15.06\%$ ($-7.73\%$ vs.\ unguided), and even at $t_{\text{start}}{=}10$, it reaches just $16.08\%$ ($-6.71\%$), indicating that insufficient denoising steps preserve real-image artifacts that degrade downstream classification. Only at $t_{\text{start}}{=}40$ does D4M marginally exceed unguided ($23.07\%$ vs.\ $22.79\%$), and even at the optimal $t_{\text{start}}$, it falls $2.63\%$ short of \cags{}'s $25.70\%$. Critically, D4M requires access to real images, introduces $t_{\text{start}}$ as an additional hyperparameter that must be tuned per dataset, and exhibits $1.5$--$2\times$ higher variance than \cags{} at every operating point. In contrast, \cags{} generates from pure noise without any real-image access, requires no $t_{\text{start}}$ tuning, and provides per-class adaptive guidance that is both more principled and more effective.

\begin{table}[t]
\centering
\caption{D4M sensitivity to $t_{\text{start}}$ on ImageNet-100 (100-class, IPC=10, ConvNet-6). New data points ($t{=}5$--$20$, $30$--$35$, $45$) use 1 seed; $t{=}25$ and $t{=}40$ use 3 seeds. D4M accuracy increases monotonically with $t_{\text{start}}$ but never reaches \cags{}'s $25.70\%$. At $t{=}5$--$20$, D4M underperforms unguided ($22.79\%$); only at $t{=}40$ does it marginally exceed unguided.}
\label{tab:d4m-sensitivity}
\begin{tabular}{ccc}
\toprule
$t_{\text{start}}$ & Top-1 (\%) & $\Delta$ vs.\ Unguided \\
\midrule
5 & $15.06$ & $-7.73$ \\
10 & $16.08$ & $-6.71$ \\
15 & $16.38$ & $-6.41$ \\
20 & \textit{XX.XX} & \textit{XX.XX} \\
25 & $20.21 \pm 0.39$ & $-2.58$ \\
30 & \textit{XX.XX} & \textit{XX.XX} \\
35 & \textit{XX.XX} & \textit{XX.XX} \\
40 & $23.07 \pm 0.59$ & $+0.28$ \\
45 & \textit{XX.XX} & \textit{XX.XX} \\
\midrule
Unguided & $22.79 \pm 0.18$ & --- \\
\cags{} (2-factor, optimal) & $\mathbf{25.70 \pm 0.07}$ & $\mathbf{+2.91}$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{D4M's benefit is dataset-dependent.}
To further investigate D4M's behavior, we apply it with $t_{\text{start}} = 25$ to two additional datasets: ImageNette (10 classes, visually distinct categories) and ImageWoof (10 classes, fine-grained dog breeds). \Cref{tab:d4m-cross} presents the results alongside the IN-100 comparison. On ImageNette, D4M achieves $54.91 \pm 0.24\%$, competitive with the four-factor \cags{} ($54.36 \pm 0.57\%$) but below the two-factor optimal ($55.27 \pm 0.68\%$). However, on ImageWoof, D4M drops to $28.05 \pm 0.27\%$, \emph{below} the unguided baseline ($31.39 \pm 0.68\%$) by $-3.34\%$. This dataset-dependent behavior reveals that D4M's real-image initialization is beneficial when classes are visually distinct (ImageNette) but harmful when classes are fine-grained and visually similar (ImageWoof), where the generative prior is more important than preserved real-image structure. \cags{}, by contrast, provides consistent improvements across all three datasets without requiring real-image access or per-dataset hyperparameter tuning, demonstrating the robustness of its principled, training-free approach. We note that D4M's ImageNette and ImageWoof results were obtained under an earlier evaluation protocol (2000 epochs); our unguided and \cags{} results for these 10-class datasets use 3000 epochs per our unified protocol, which may partially account for the performance difference.

\begin{table}[t]
\centering
\caption{D4M ($t_{\text{start}}{=}25$) across datasets. D4M is competitive with four-factor \cags{} on ImageNette but underperforms unguided on both ImageWoof (fine-grained) and IN-100 (large-scale). The two-factor optimal \cags{} provides consistent improvements across all datasets without real-image access. $\dagger$ indicates D4M results from an earlier 2000-epoch protocol.}
\label{tab:d4m-cross}
\begin{tabular}{lccccc}
\toprule
\textbf{Dataset} & \textbf{Classes} & \textbf{Unguided} & \textbf{\cags{} 4-fac.} & \textbf{\cags{} opt.} & \textbf{D4M ($t{=}25$)$^\dagger$} \\
\midrule
ImageNette & 10 & $51.30 \pm 0.41$ & $54.36 \pm 0.57$ & $\mathbf{55.27 \pm 0.68}$ & $54.91 \pm 0.24$ \\
ImageWoof & 10 & $31.39 \pm 0.68$ & $29.76 \pm 0.14$ & $\mathbf{32.87 \pm 0.29}$ & $28.05 \pm 0.27$ \\
IN-100 & 100 & $22.79 \pm 0.18$ & $25.29 \pm 0.27$ & $\mathbf{25.70 \pm 0.07}$ & $20.21 \pm 0.39$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Comparison with 2026 methods: DMGD and LGD.}
DMGD~\cite{wang2026dmgd} (CVPR~2026) and LGD~\cite{chansantiago2026learnabilityguided} represent the latest advances in diffusion-based dataset distillation. DMGD introduces a training-free dual matching framework combining semantic matching via classifier-free guidance and distribution matching via optimal transport, while LGD proposes an incremental learnability-driven approach that constructs datasets stage-by-stage with model-conditioned sample selection. \Cref{tab:sota-comparison} compares our results with DMGD's published numbers on ImageNette and ImageWoof at IPC=10 with ResNetAP-10, alongside LGD's reported numbers at IPC=50.

\begin{table}[t]
\centering
\caption{Comparison with 2026 state-of-the-art methods on ImageNette and ImageWoof. DMGD numbers are from~\cite{wang2026dmgd} (ResNet10-AP, IPC=10). LGD numbers are from~\cite{chansantiago2026learnabilityguided} (ConvNet-6, IPC=50). Our results use ResNetAP-10 for the IPC=10 comparison and ConvNet-6 for the IPC=50 comparison. Our unguided baseline is lower than DMGD's DiT baseline, likely due to our heavier augmentation protocol (color jitter, PCA lighting, CutMix with $p{=}1.0$); nevertheless, our relative improvement over the respective baseline is comparable.}
\label{tab:sota-comparison}
\begin{tabular}{llcc}
\toprule
\textbf{Method} & \textbf{Protocol} & \textbf{ImageNette} & \textbf{ImageWoof} \\
\midrule
\multicolumn{4}{l}{\emph{IPC=10, ResNetAP-10 (from DMGD~\cite{wang2026dmgd})}} \\
DiT (unguided) & published & $59.1$ & $34.7$ \\
Minimax & published & $62.0$ & $39.2$ \\
MGD$^3$ & published & $66.4$ & $40.4$ \\
DMGD & published & $\mathbf{68.4}$ & $\mathbf{40.8}$ \\
\midrule
\multicolumn{4}{l}{\emph{IPC=10, ResNetAP-10 (ours)}} \\
Unguided & ours & $54.00 \pm 0.32$ & $36.83 \pm 0.25$ \\
\cags{} (2-factor opt.) & ours & $62.00 \pm 0.16$ & $38.86 \pm 0.19$ \\
\midrule
\multicolumn{4}{l}{\emph{IPC=50, ConvNet-6 (from LGD~\cite{chansantiago2026learnabilityguided})}} \\
DiT (unguided) & published & $74.1$ & $48.5$ \\
MGD$^3$ & published & $80.9$ & $53.4$ \\
LGD & published & $\mathbf{82.6}$ & $\mathbf{53.9}$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Protocol differences affect absolute comparison.}
Our unguided ResNetAP-10 baseline on ImageNette ($54.00\%$) is $5.1\%$ below DMGD's reported DiT baseline ($59.1\%$), despite using the same pretrained DiT-XL/2 generator. We attribute this gap primarily to our heavier data augmentation: following the MGD$^3$ protocol, we apply random resized crop, horizontal flip, color jitter ($0.4$ brightness, contrast, saturation), AlexNet-style PCA lighting noise, and CutMix ($p{=}1.0$), whereas DMGD and LGD employ lighter augmentation (random crop and CutMix). At IPC=10, where each class has only 10 training images, aggressive color and lighting augmentation can destroy discriminative features, particularly on visually distinct datasets like ImageNette. Despite this baseline difference, our two-factor \cags{} achieves a relative improvement of $+14.8\%$ over our unguided baseline on ImageNette ($54.00\% \to 62.00\%$), comparable to DMGD's $+15.7\%$ improvement over its DiT baseline ($59.1\% \to 68.4\%$). On ImageWoof, our unguided baseline ($36.83\%$) is actually $2.1\%$ above DMGD's DiT baseline ($34.7\%$), and our \cags{} optimal ($38.86\%$) approaches Minimax ($39.2\%$).

\paragraph{Complementary positioning.}
\cags{} occupies a complementary position relative to DMGD and LGD. DMGD's dual matching requires computing optimal transport distances between synthetic and target distributions at each denoising step, adding computational overhead. LGD requires iterative classifier training and learnability-guided sample selection, fundamentally departing from the single-pass training-free paradigm. \cags{}, by contrast, adjusts only the per-class guidance strength $\lambda$ based on a one-time complexity analysis, adding negligible overhead to the diffusion sampling process. The three approaches target different aspects of the generation pipeline: DMGD improves distribution alignment, LGD improves sample selection, and \cags{} improves per-class guidance calibration. These mechanisms are orthogonal: \cags{} could serve as the per-class guidance module within DMGD's framework or as the seed generator for LGD's incremental construction, potentially combining adaptive guidance with distribution matching or learnability-driven selection.
