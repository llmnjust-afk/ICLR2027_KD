# TAGS × 引导窗口 × 引导时长 三因素交互实验报告

日期：2026-09-01
硬件：2 × NVIDIA RTX 5090（32 GB GDDR7），torch 2.11.0+cu128
数据集：ImageNette（10 类），IPC = 10，DiT-XL/2-256
配置总数：22（4 条曲线）
评估总次数：**330**（每配置 5 个独立数据集 × 3 个训练 seed = 15 runs）
运行时长：约 2 小时（双卡并行）

---

## 1. 本轮目标

上一轮 duration sweep（单 seed，8 配置）给出两条初步结论：低噪声窗口在短时长下大幅领先、约 25 步处出现方向交叉。但该结论存在两个未验证的薄弱点：单一训练 seed 无法区分「配置差异」与「训练噪声」；且 TAGS（cosine 权重调度）始终处于关闭状态，其增益从未在受控条件下测得。历史报告曾出现 31.61% 的 TAGS 结果，但缺少同条件 constant 对照，无法归因。本轮以三个因素的完整交叉设计回答上述问题：引导窗口方向（低噪声 / 高噪声）× 引导时长（10–35 步）× 权重形状（constant / cosine），并对每个配置施加 3 个训练 seed 以获得可做配对检验的样本。

## 2. 实验设计

引导窗口方向决定 guidance 施加于扩散轨迹的哪一段：低噪声窗口对应 `i < t_stop`（靠近干净图像端），高噪声窗口对应 `i > t_stop`（靠近纯噪声端）。两者在相同 `guidance_steps` 下施加的引导步数严格相等，由 `AdaptiveStopTiming.compute_stop()` 按窗口方向转换得到，因此时长可跨方向公平比较。权重形状中 constant 在窗口内施加恒定强度，cosine（TAGS）采用 late-rising 形状——权重在 t→0 处最大、向窗口边界单调衰减至零。引导强度上限由 CAGS 按类复杂度给出，本数据集下落在 0.14–0.19 区间。

时长取值为 {10, 15, 18, 20, 22, 25, 35}，其中 18/20/22 为本轮新增，用于填补上一轮 15→25 之间的空隙并定位交叉点。低噪声方向在 constant 下覆盖全部 7 个时长、在 cosine 下覆盖 6 个；高噪声方向在 constant 下覆盖 7 个，cosine 下取 18 与 25 两点作为机制对照。每个配置生成 5 个独立数据集（各 100 张图，10 类 × IPC 10），每个数据集用 3 个 seed 训练 ConvNet 分类器（20 epochs，batch 32），在 ImageNette 验证集（3925 张）上报告 Top-1。

## 3. 主结果

| Steps | Low-noise constant | Low-noise **TAGS** | High-noise constant | High-noise TAGS |
|:-:|:-:|:-:|:-:|:-:|
| 10 | 26.20 ± 1.35 | 26.95 ± 1.51 | 17.45 ± 1.39 | — |
| 15 | 25.85 ± 1.48 | 26.91 ± 1.61 | 18.53 ± 1.10 | — |
| 18 | 24.58 ± 1.27 | **27.34 ± 0.95** | 20.49 ± 1.44 | 20.56 ± 1.29 |
| 20 | 23.59 ± 1.28 | 26.09 ± 1.71 | 19.99 ± 2.04 | — |
| 22 | 22.89 ± 0.97 | 25.70 ± 1.27 | 20.84 ± 1.72 | — |
| 25 | 21.07 ± 1.72 | 24.85 ± 1.87 | **21.75 ± 1.36** | 20.92 ± 2.48 |
| 35 | 20.08 ± 1.86 | — | 21.11 ± 1.31 | — |

全局最优配置为 **低噪声 + TAGS + 18 步 = 27.34 ± 0.95%**，同时具有全部配置中最小的标准差。相较同条件 constant（24.58%）提升 2.76 pp，相较上一轮报告的最佳单 seed 结果（25.90%）提升 1.44 pp。

## 4. 发现一：TAGS 增益随引导时长单调放大

配对 t 检验（同 dataset、同 seed 配对，消除共同噪声）给出 TAGS 相对 constant 的增益随时长变化的清晰趋势：

| Steps | Δ (pp) | p | 显著性 | 胜出比 |
|:-:|:-:|:-:|:-:|:-:|
| 10 | +0.74 | 0.165 | n.s. | 10/15 |
| 15 | +1.06 | 0.023 | * | 11/15 |
| 18 | +2.76 | < 0.001 | *** | 15/15 |
| 20 | +2.50 | < 0.001 | *** | 14/15 |
| 22 | +2.81 | < 0.001 | *** | 15/15 |
| 25 | +3.78 | < 0.001 | *** | 15/15 |

在 10 步处增益尚不显著（+0.74 pp，p = 0.165），到 18 步后增益稳定在 2.5–3.8 pp 且全部达到 p < 0.001，25 步处达到最大的 +3.78 pp。这一模式指向明确的机制解释：constant 权重在长窗口内持续施加固定强度引导，累积的引导量随时长线性增长并最终压过样本多样性，表现为 constant 曲线从 26.20% 单调衰减至 20.08%；而 cosine 的衰减形状使总引导量随时长呈次线性增长，把过度引导的拐点向后推移。因此 TAGS 的真实作用并非在最优时长上再榨取精度，而是**显著拓宽可用时长区间**——低噪声 constant 仅在 10–15 步区间维持 25% 以上，加入 TAGS 后 10–22 步全区间均保持 25.7% 以上。这对实践具有直接价值，因为它降低了系统对 IAST 时长预测精度的敏感性。

值得注意的是，TAGS 曲线自身在 18 步处取得峰值而非在最短时长处，说明 cosine 并非简单地「削弱引导」。若仅是等效地降低平均强度，其最优点应与 constant 同样落在 10 步。峰值后移表明衰减形状让模型得以在更长的窗口里累积有效的类别对齐，同时避免末段过度锐化。

## 5. 发现二：TAGS 的有效性依赖窗口方向

高噪声方向的 TAGS 对照给出接近零的增益，与低噪声方向形成鲜明对比：

| 窗口 | Steps | constant | TAGS | Δ (pp) | p |
|:-:|:-:|:-:|:-:|:-:|:-:|
| Low | 18 | 24.58 | 27.34 | **+2.76** | < 0.001 |
| High | 18 | 20.49 | 20.56 | +0.07 | 0.887 |
| Low | 25 | 21.07 | 24.85 | **+3.78** | < 0.001 |
| High | 25 | 21.75 | 20.92 | −0.82 | 0.316 |

在高噪声窗口下 TAGS 于 18 步处仅带来 +0.07 pp（p = 0.887，8/15 胜出，等同随机），在 25 步处甚至略微下降 0.82 pp。这构成本轮最强的机制证据：late-rising cosine 的权重峰值位于 t→0，恰好落在低噪声窗口内信息密度最高、语义结构最终确定的时段，因此调制精准作用于决定样本可判别性的关键步；而在高噪声窗口内，权重峰值位于窗口远离 t=0 的一侧边界，此时潜变量仍以噪声为主，引导强度的时变塑形无法转化为语义层面的差异，调制退化为无效。换言之，TAGS 与低噪声窗口并非两个可独立叠加的模块，而是存在必要的协同——这一点应在论文中作为设计约束明确陈述，而非将两者呈现为正交贡献。

## 6. 发现三：交叉点定位于 25 步附近

窗口方向的优劣随时长反转，配对检验将交叉点定位在 25 步：

| Steps | Low − High (pp) | p | 结论 |
|:-:|:-:|:-:|:-:|
| 10 | +8.75 | < 0.001 | 低噪声显著优，15/15 |
| 15 | +7.32 | < 0.001 | 低噪声显著优，15/15 |
| 18 | +4.09 | < 0.001 | 低噪声显著优，15/15 |
| 20 | +3.59 | < 0.001 | 低噪声显著优，15/15 |
| 22 | +2.05 | < 0.001 | 低噪声显著优，15/15 |
| 25 | −0.67 | 0.210 | **两方向等效（交叉点）** |
| 35 | −1.02 | 0.030 | 高噪声显著优，3/15 |

低噪声优势从 10 步的 +8.75 pp 单调收窄至 22 步的 +2.05 pp，在 25 步处差异不再显著（p = 0.210），到 35 步高噪声以 1.02 pp 反超（p = 0.030）。两条 constant 曲线的形状相反——低噪声单调下降、高噪声单调上升至 25 步峰值——反映出两个窗口对引导预算的需求截然不同：低噪声段每一步引导的边际效力高，少量步数即饱和；高噪声段单步效力低，需累积足够步数才能形成类别对齐，但其精度上限（21.75%）始终低于低噪声的最优（27.34%）。因此交叉点的存在不构成对高噪声方向的支持，它只说明在时长被强制设得过长时高噪声受损较轻。实际配置应当取低噪声 + TAGS + 18 步，此时距交叉点尚有充分余量。

需要说明的是高噪声 constant 曲线在 18→20 步出现 20.49% → 19.99% 的非单调回落，随后在 22/25 步继续上升。该回落幅度（0.5 pp）小于该点标准差（约 1.4–2.0 pp），应视为噪声而非真实结构。

## 7. 与历史报告的差异

历史 TAGS 报告中 31.61% 的结果在本轮受控条件下未能复现。本轮低噪声 + cosine 在 12–18 步区间的实测值为 26.9–27.3%，与该数字相差约 4 pp。造成差异的可能来源包括：历史实验使用单一 seed 与单一数据集，其波动范围（本轮观察到 seed 间可达 ±1.5 pp、dataset 间可达 ±2 pp）不足以解释 4 pp 的差距；更可能的原因是历史实验运行于评估管线修复之前，或采用了不同的 epoch / batch 配置。鉴于本轮 15 runs 的统计量与全部通过的显著性检验，应以 27.34 ± 0.95% 作为该配置的可信值，并在论文中放弃历史数字。

同时本轮修正了上一轮报告中「低噪声在 15 步后退化、故最优为 10–15 步」的表述。该结论仅对 constant 成立；在 TAGS 开启后最优时长后移至 18 步，且 10–22 步全区间保持高位。上一轮结论受限于 TAGS 关闭的实验条件。

## 8. 每 seed 分解与统计可靠性

seed 之间存在系统性偏移：在低噪声 constant 的 7 个时长上，seed 1 平均高于 seed 0 约 1.0 pp、高于 seed 2 约 2.3 pp，且该顺序在几乎所有配置中保持一致。这意味着单 seed 实验的绝对值不可直接跨报告比较，但配置间的相对排序在所有 seed 下均保持不变——低噪声 constant 的 d10 > d15 > d18 > d20 > d22 > d25 > d35 在三个 seed 中各自独立成立。这支持本轮全部结论的稳健性，同时说明后续实验必须继续使用 ≥ 3 seed 报告。

TAGS 最优配置（低噪声 d18）同时具有最小标准差 0.95%，低于低噪声 constant 各点的 0.97–1.86%。除精度增益外，cosine 调度似乎也降低了跨数据集与跨 seed 的方差，这一点若在其他数据集上复现，可作为额外的方法学优势论证。

## 9. 复现命令

```bash
# 低噪声 constant 全时长
python3 duration_sweep.py --window low_noise --schedule constant \
    --durations 10 15 18 20 22 25 35 --seeds 0 1 2

# 低噪声 TAGS（最优方向）
python3 duration_sweep.py --window low_noise --schedule cosine \
    --durations 10 15 18 20 22 25 --seeds 0 1 2

# 高噪声对照
python3 duration_sweep.py --window high_noise --schedule constant \
    --durations 10 15 18 20 22 25 35 --seeds 0 1 2
python3 duration_sweep.py --window high_noise --schedule cosine \
    --durations 18 25 --seeds 0 1 2

# 汇总与作图
python3 analyze_sweep.py --base ./results/sweep
python3 plot_sweep.py --summary ./results/sweep/summary.json --out ./figures
```

结果文件：`results/sweep/sweep_{low,high}_noise_{const,cosine}_results.json`、
汇总：`results/sweep/summary.json`、图：`figures/duration_sweep.{pdf,png}`。

## 10. 后续工作

当前所有结论均建立在 ImageNette、IPC = 10 这一单点上，外部效度尚未建立。优先级最高的验证是在 IPC = 50 与 100 下重复低噪声 constant 与 TAGS 的时长曲线：IAST 的设计前提是最优时长随 IPC 单调递增，而本轮仅在单一 IPC 上测得 18 步这一最优值，无法检验该单调性假设，也无法排除 18 步只是 IPC = 10 的特例。其次应扩展至 ImageWoof 以检验细粒度类别下的行为，该数据集类间距离更小，低噪声窗口的语义塑形作用可能更关键，是对发现二的自然强化测试。

方法层面尚有两处空白。其一，本轮仅比较 constant 与 cosine 两种形状，而 `TimestepAdaptiveSchedule` 还支持 linear 与 exponential；既然增益来源被归因于「衰减形状抑制累积引导量」，则不同衰减速率应产生可预测的增益排序，这是一个能直接检验机制解释的低成本实验。其二，IAST 当前输出的时长与本轮实测最优（18 步）是否一致尚未核对，若 IAST 在 IPC = 10 下预测值偏离 18 步较远，则其 lambda = 0.316 需要重新校准。

工程侧仍缺 FID 指标实现、ImageNet-100/1K 与 CIFAR 系列实验、论文 .tex/.bib 文件，以及若干硬编码 `/ssd_data/` 路径的清理。
