# 新手运行 AGS-DD 完整指南

## 总览：项目在做什么

这个项目用预训练的扩散模型（DiT-XL/2）来"蒸馏"数据集——把 ImageNet 的几千张图片压缩成每类只有 10~50 张合成图片，然后用这些合成图片训练分类器，在真实测试集上测准确率。三个自适应模块（CAGS/IAST/TAGS）控制扩散采样过程中的引导策略。

整个流程分三个阶段：**① 环境搭建 → ② 生成蒸馏数据 → ③ 评估准确率**

---

## 第一步：安装环境

```bash
# 1. 克隆代码
git clone https://github.com/llmnjust-afk/ICLR2027_KD.git
cd ICLR2027_KD

# 2. 创建 conda 环境（README 写的 Python 3.8，但 3.9/3.10 也可以）
conda create -n ags_dd python=3.10 -y
conda activate ags_dd

# 3. 安装基础依赖
pip install -r requirements.txt

# 4. ⚠️ 补装 requirements.txt 中缺失的 4 个依赖（代码里 import 了但没列出）
pip install timm efficientnet_pytorch scikit-image pandas
```

> **注意**：`requirements.txt` 里列了 `diffusers>=0.25.0`，但项目自带了一个修改过的 `diffusers/` 目录（29MB），Python 会优先使用本地这个版本。这是正常的——MGD³ 基线需要修改过的 diffusers 库。

---

## 第二步：准备数据集

**建议新手从 ImageNette 开始**（10类，仅 ~1.5GB，下载快，跑完一个实验约 25 分钟）。

```bash
# 下载 ImageNette（官方链接）
wget https://s3.amazonaws.com/fast-ai-imageclas/imagenette2.tgz
tar -xzf imagenette2.tgz

# 解压后目录结构：
# imagenette2/
#   train/
#     n01440764/  ← 文件夹名是 WordNet ID（和 misc/class_nette.txt 对应）
#       n01440764_*.JPEG
#     n02102040/
#     ...（共 10 个类）
#   val/
#     n01440764/
#     ...（同样 10 个类）
```

如果还想跑 ImageWoof（10类细粒度犬种，难度更高）：
```bash
wget https://s3.amazonaws.com/fast-ai-imageclas/imagewoof2.tgz
tar -xzf imagewoof2.tgz
```

> **关键**：数据集的文件夹名（如 `n01440764`）必须和 `misc/class_nette.txt` 里的类名一一对应。ImageNette/ImageWoof 官方下载就是这个格式，不需要额外处理。

---

## 第三步：获取预训练模型（自动下载）

首次运行代码时会**自动下载**两个模型，无需手动操作，但需要联网：

| 模型 | 大小 | 来源 | 用途 |
|------|------|------|------|
| `DiT-XL-2-256x256.pt` | ~1.1 GB | `dl.fbaipublicfiles.com/DiT/models/` | 扩散模型本体 |
| `sd-vae-ft-mse` | ~335 MB | `stabilityai/sd-vae-ft-mse` (HuggingFace) | VAE 编解码器 |

下载后自动存到 `pretrained_models/` 和 HuggingFace 缓存目录。

> 如果网络不好，可以手动下载 DiT 检查点放到 `pretrained_models/DiT-XL-2-256x256.pt`。

---

## 第四步：Smoke Test（验证管道能跑通）

先修改 `smoke_test.py` 里的数据路径：

```bash
# 编辑 smoke_test.py 第 6 行，把路径改成你的 imagenette2 路径
# 例如：
#   "--imagenet-dir", "/home/你的用户名/datasets/imagenette2/",
```

然后运行：
```bash
python smoke_test.py
```

这会只生成 1 张图、5 个采样步骤，用来验证整个管道（模型加载 → VAE特征提取 → 聚类 → 扩散采样 → 保存图片）能跑通。成功的话会在 `./test_output/smoke/` 下看到图片。

---

## 第五步：生成蒸馏数据集

这是核心实验步骤。用 ImageNette、IPC=10（每类10张）跑：

```bash
# 方法 A：用 shell 脚本（推荐，自动设置好了参数）
IMAGENET_DIR=/你的路径/imagenette2/ bash scripts/run_imagenette.sh 10

# 方法 B：直接用 Python 命令
python sample_ags.py \
    --spec nette \
    --num-samples 10 \
    --save-dir ./generated/ags_dd/nette_ipc10 \
    --num-datasets 5 \
    --imagenet-dir /你的路径/imagenette2/ \
    --cags-alpha 0.5 --cags-beta 0.5 \
    --cags-kmin 2 --cags-kmax 20 \
    --guidance-scale-min 0.05 --guidance-scale-max 0.5 \
    --iast-lambda 0.1 --iast-min-stop 5 --iast-max-stop-ratio 0.9 \
    --tags-schedule cosine
```

**会发生什么**：
1. 下载/加载 DiT-XL/2 模型 + VAE（约 30 秒）
2. 遍历 ImageNette train 集的所有图片，用 VAE encoder 提取特征（约 1.5 分钟，9469 张图）
3. 对每个类做 K-means 聚类，计算复杂度分数（CAGS）
4. 根据 IPC 计算每个类的停止时机（IAST）
5. 用扩散模型 + 自适应引导生成合成图片（50 步 DDIM 采样 × 10 类 × 10 张 × 5 个数据集 ≈ 25 分钟）
6. 图片保存到 `./generated/ags_dd/nette_ipc10/dataset_0/` 到 `dataset_4/`

**预计时间**：单卡 RTX 4090 约 25 分钟，RTX 3060 约 60 分钟。

---

## 第六步：评估准确率（论文需要的核心结果）

生成完蒸馏数据后，需要训练分类器测量准确率：

```bash
python -m evaluation.train_eval \
    --train-dir ./generated/ags_dd/nette_ipc10/dataset_0 \
    --test-dir /你的路径/imagenette2/val \
    --arch convnet7 \
    --num-classes 10 \
    --epochs 2000 \
    --num-seeds 5 \
    --save-dir ./results/eval \
    --dataset-name imagenette \
    --ipc 10
```

**会发生什么**：
1. 加载蒸馏数据集（10类×10张=100张合成图片）
2. 训练一个 ConvNet-7 分类器 2000 个 epoch（数据量极小，每个 epoch 很快）
3. 在 ImageNette val 集上测试 Top-1/Top-5 准确率
4. 用 5 个随机种子重复 5 次
5. 输出平均准确率 ± 标准差

**预计时间**：单卡约 10-20 分钟（分类器很小，瓶颈是 2000 epoch × 5 seeds）。

---

## 第七步（可选）：跑完整实验矩阵

| 实验 | 命令 | 时间 |
|------|------|------|
| ImageNette IPC=50 | `IMAGENET_DIR=... bash scripts/run_imagenette.sh 50` | ~2h |
| ImageWoof IPC=10 | `IMAGENET_DIR=... bash scripts/run_imagewoof.sh 10` | ~25min |
| MGD³ 基线对比 | `python sample_ags.py --spec nette --num-samples 10 --baseline --imagenet-dir ... --save-dir ./generated/baseline` | ~25min |
| 消融实验（8种配置） | `python run_ablation.py --spec nette --num-samples 10 --test-dir .../imagenette2/val` | ~2h |
| 跨架构评估 | `bash scripts/run_cross_arch.sh ./generated/ags_dd/nette_ipc10/dataset_0 .../imagenette2/val 10` | ~1h |

---

## 常见问题排查

**Q1: `ModuleNotFoundError: No module named 'timm'`**
→ 漏装了，运行 `pip install timm`

**Q2: `ModuleNotFoundError: No module named 'efficientnet_pytorch'`**
→ 运行 `pip install efficientnet_pytorch`

**Q3: 下载 DiT 模型很慢或失败**
→ 手动下载：
```bash
mkdir -p pretrained_models
wget https://dl.fbaipublicfiles.com/DiT/models/DiT-XL-2-256x256.pt -O pretrained_models/DiT-XL-2-256x256.pt
```

**Q4: HuggingFace VAE 下载失败（网络问题）**
→ 设置镜像或手动下载：
```bash
export HF_ENDPOINT=https://hf-mirror.com  # 国内镜像
# 或者手动下载到本地后修改代码用 from_pretrained 加载本地路径
```

**Q5: `CUDA out of memory`**
→ 显存不够。降低 `--num-sampling-steps`（从 50 降到 25），或确保 batch_size=1（默认就是 1）。最低需要 12GB 显存。

**Q6: `AssertionError: spec does not exist!`**
→ 数据路径不正确。确保 `--imagenet-dir` 指向的目录下有 `train/` 子文件夹，且 `train/` 下有对应类名的文件夹。

**Q7: 跑评估时报 `FileNotFoundError`**
→ `--train-dir` 要指向具体的数据集目录（如 `dataset_0`），不是上层目录。

---

## 需要修改的硬编码路径汇总

新手跑通代码需要修改这些地方的路径（把 `/ssd_data/...` 改成自己的路径）：

| 文件 | 行号 | 默认值 | 改成 |
|------|------|--------|------|
| `smoke_test.py` | 6 | `/ssd_data/imagenette/imagenette2/` | 你的 imagenette2 路径 |
| `sample_ags.py` | 81 | `/ssd_data/imagenet/` | 你的数据集路径 |
| `scripts/run_imagenette.sh` | 9 | `/ssd_data/imagenet/` | 你的 imagenette2 路径 |
| `scripts/run_imagewoof.sh` | 9 | `/ssd_data/imagenet/` | 你的 imagewoof2 路径 |
| `scripts/run_ablation.sh` | 7 | `/ssd_data/imagenet/val` | 你的 val 路径 |
| `scripts/run_cross_arch.sh` | 8 | `/ssd_data/imagenet/val` | 你的 val 路径 |

> **最简方案**：只用 `sample_ags.py` 的 `--imagenet-dir` 参数传路径，不需要改源码。Shell 脚本里也可以通过环境变量 `IMAGENET_DIR=...` 覆盖。

---

## 最低硬件要求

| 阶段 | 最低显存 | 推荐显卡 |
|------|---------|---------|
| 生成蒸馏数据（DiT推理） | 12 GB | RTX 3060 12GB |
| 评估准确率（训练分类器） | 4 GB | 任何 CUDA 显卡 |
| 跑完整实验矩阵 | 16-24 GB | RTX 4080/4090 |
| ImageNet-1K 全量实验 | 24 GB+ | A100/4090 双卡 |
