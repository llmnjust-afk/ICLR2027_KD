# AGS-DD 当前研究思路、创新与实验结果审阅稿

> 文档状态：完整更新（2026-09-03 11:11 UTC，全部实验已完成）  
> 目标投稿：ICLR 2027  
> 工作名称：Adaptive Guidance Scheduling for Training-Free Diffusion Dataset Distillation（AGS-DD）

## 1. 一句话概括

本工作以 MGD³ 的训练自由扩散数据集蒸馏流程为核心 baseline，将"所有类别、所有去噪时刻和所有 IPC 预算共用同一引导配置"的做法改造成类别自适应（CAGS）、时间步自适应（TAGS）和预算自适应（IAST）的生成策略。全部实验已完成：4 个数据集 × 3 个 IPC × 3 个架构、完整模块消融、FID 分析和 Random Herding 基线。CAGS 在所有数据集和 IPC≥10 的设置中一致超过 unguided，跨架构增益显著（ResNet-18 +20.8%，ResNetAP-10 +19.9%）；TAGS 在 CAGS 上进一步提供时间维度增益（100类 +9.0%）；IAST 通过引导范围缩放保护低预算场景，在 10 类 IPC=1 上将 CAGS 的增益从 +11.8% 提升至 +18.1%，并在 100 类 IPC=1 上将 CAGS 的损害从 −8.0% 缓解至 −4.9%。

## 2. 研究问题与核心假设

### 2.1 研究问题

MGD³ 使用冻结的预训练 DiT 直接生成类别条件合成图像，不需要优化生成模型或合成图像，因此具有训练自由、单次生成和易扩展的优势。然而，其生成阶段使用全局固定的 CFG 强度，并采用固定的高噪声生成窗口。这种统一配置忽略了三个差异：

1. 不同类别的类内变化、类间可分性和视觉模态数不同；
2. CFG 在不同扩散时间步上的作用不同；
3. IPC=1 与 IPC=50 对单张样本代表性和集合多样性的需求不同。

### 2.2 核心假设

本工作的核心假设是：数据集蒸馏所需的最优引导并非常数，而是类别、时间步和 IPC 预算的函数。固定引导可能在简单类别上过度锐化并损失多样性，同时在复杂类别上引导不足；这种冲突会随着类别数量和类别异质性的增加而更加明显。若能针对这些差异自适应地控制生成过程，就能在不训练 DiT、不增加可学习参数的前提下，提高合成数据训练下游分类器的有效性。

## 3. Baseline 与比较对象

### 3.1 核心方法 baseline：MGD³

本工作的直接方法 baseline 是 MGD³ 的训练自由扩散数据集蒸馏范式。本文不改变 DiT 参数，而是在 MGD³ 的采样阶段修改引导策略。内部对照包括：

- **Unguided**：`lambda = 0`，即无 CFG 的 MGD³ 生成；
- **Fixed lambda**：所有类别与时间步使用同一个 CFG 强度；
- **CAGS v1**：早期版本，用于说明复杂度建模的演进；
- **CAGS v2**：当前主要方法，权重 `(0.15, 0.15, 0.35, 0.35)`，sigmoid 参数 `slope=5.0, center=0.5`。

### 3.2 数据选择 baseline：Random Herding

Random Herding 从真实训练集中按类别选取给定 IPC 的图像，用于回答"直接保留少量真实图像"与"由冻结扩散模型生成合成图像"之间的差异。所有 Random Herding 结果已通过顺序执行和独立日志完成，无日志污染问题。

### 3.3 外部数据集蒸馏方法

论文计划与 D4M、SRe²L、MTT、DMGD、CoDA、IDC-1 和 MinMaxDiff 等已发表方法比较。当前正文仅列出部分方法名称，尚未形成经过统一协议核对的完整对比表，仍为计划中的外部参照。

## 4. 方法创新

### 4.1 CAGS：类别自适应引导强度

CAGS 先从真实训练图像提取 VAE 特征，再为每个类别估计复杂度，最后把归一化复杂度映射为类别专属 CFG 强度。复杂度指标综合类内方差、类间可分性、结构多样性和聚类分散度。复杂类别获得更强引导以突出判别特征；简单类别获得较弱引导以避免过度锐化。复杂度只需离线计算一次并缓存，不引入模型训练。

核心创新是把 CFG 从数据集级超参数提升为类别级控制变量。100 类实验提供了最直接证据：固定 `lambda=0.10` 低于无引导，而 CAGS 在相同生成框架下超过无引导。

### 4.2 TAGS：时间步自适应引导调度

TAGS 将常数引导改为随扩散时间步变化的函数，考察 cosine/annealed、linear 和 exponential 三类调度。新协议下，100 类 IPC=10 的 linear TAGS+CAGS 达到 `13.38 ± 0.13%`，比 CAGS alone 高 0.71 个百分点，比 unguided 高 1.11 个百分点，且标准差仅 0.13%（所有配置中最低）。Exponential TAGS+CAGS 均值同为 13.38% 但标准差 0.51%，因此 Linear 为优选默认配置。

TAGS-only 在 10 类上未超过 unguided（旧协议结果），说明时间维度调节在类别异质性有限时价值有限；但其与 CAGS 组合后效果显著，表明时间适配需要类别校准为基础。

### 4.3 IAST：IPC 自适应引导范围（重新设计）

原始 IAST 设计为根据 IPC 调整去噪起始时间步 t_start，但分析发现其在 IPC=1 和 IPC=10 之间几乎不改变 t_start（sqrt(IPC) 在小值范围内变化极慢），实际上是空操作。

重新设计后的 IAST 改为根据 IPC 缩放 CAGS 的引导范围：

```text
range_scale(IPC) = min(1.0, log(IPC+1) / log(IPC_ref+1))   # IPC_ref = 10
[lambda_min_eff, lambda_max_eff] = range_scale * [lambda_min, lambda_max]
```

各 IPC 下的效果：

| IPC | range_scale | 100类 [0, 0.06] → 实际范围 | 10类 [0, 0.08] → 实际范围 |
|---:|---:|---:|---:|
| 1 | 0.289 | [0, 0.017] | [0, 0.023] |
| 10 | 1.000 | [0, 0.06]（不变） | [0, 0.08]（不变） |
| 50 | 1.000 | [0, 0.06]（不变） | [0, 0.08]（不变） |

设计原理：低 IPC 时每类只有少量合成图像，无法可靠地利用逐类引导差异；IAST 自动减弱自适应强度，而不是强制施加完整的逐类控制。IPC≥10 时 IAST 完全透明，现有结果无需重跑。

实验验证：
- 10 类 IPC=1：IAST+CAGS `22.67 ± 0.81%`，比 CAGS alone `21.47 ± 1.67%` 高 1.20 个百分点，标准差从 1.67 降至 0.81；
- 100 类 IPC=1：IAST+CAGS `4.04 ± 0.06%`，比 CAGS alone `3.91 ± 0.24%` 高 0.13 个百分点，损害从 −8.0% 缓解至 −4.9%，标准差从 0.24 降至 0.06。

### 4.4 统一 AGS 采样器

完整 AGS 将三个控制维度组合：IAST 缩放 CAGS 引导范围，CAGS 在缩放后的范围内决定类别级基础引导，TAGS 决定该引导沿时间步如何变化。整个过程使用冻结 DiT，不计算梯度、不更新权重。

## 5. 当前实验协议

### 5.1 下游评估设置

- 优化器：SGD，初始学习率 0.1，权重衰减 `1e-4`，batch size 128；
- 所有下游架构均使用 instance normalization；
- 训练过程中记录最佳 Top-1（不是最终 epoch）；
- 每个配置使用 3 个随机种子 `0, 1, 2`；
- 10 类 IPC=1/10/50 分别训练 3000/2000/1500 epochs；
- 100 类 IPC=1/10/50 分别训练 2000/1300/1000 epochs。

### 5.2 实验范围（已全部完成）

- 数据集：ImageNet-100 的 10 类子集、完整 ImageNet-100、ImageNette、ImageWoof；
- IPC：1、10、50；
- 架构：ConvNet-6、ResNet-18、ResNetAP-10；
- 额外分析：CAGS/TAGS/IAST 消融、TAGS 调度对比、FID、Random Herding 基线。

## 6. 全部已完成正式结果

除特别说明外，下表均为 ConvNet-6、instance normalization、3 个种子和训练过程最佳 Top-1。

### 6.1 ImageNet-100 的 10 类子集

| IPC | Random Herding | Unguided | Fixed `lambda=0.05` | CAGS v2 | IAST+CAGS v2 |
|---:|---:|---:|---:|---:|---:|
| 1 | 14.67 ± 0.66 | 19.20 ± 0.49 | — | 21.47 ± 1.67 | **22.67 ± 0.81** |
| 10 | 34.07 ± 0.90 | 39.07 ± 1.16 | **41.87 ± 0.25** | 40.87 ± 0.98 | — |
| 50 | 48.33 ± 0.50 | 48.80 ± 0.82 | — | **53.47 ± 0.82** | — |

CAGS 在三个 IPC 上均超过 unguided。IAST+CAGS 在 IPC=1 上比 CAGS alone 高 1.20 个百分点，标准差减半。10 类 IPC=10 上最佳固定 `lambda=0.05` 比 CAGS 高 1.00 个百分点，说明类别较少且异质性有限时全局参数仍可能有效。Random Herding 与 unguided 的差距随 IPC 增加而收窄：IPC=1/10/50 分别相差 4.53、5.00 和 0.47 个百分点，但 CAGS 在 IPC=50 重新把优势扩大至 5.14 个百分点。

### 6.2 完整 ImageNet-100

| IPC | Random Herding | Unguided | Fixed `lambda=0.10` | CAGS v2 | IAST+CAGS v2 | TAGS(linear)+CAGS |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 3.28 ± 0.15 | **4.25 ± 0.12** | — | 3.91 ± 0.24 | 4.04 ± 0.06 | — |
| 10 | 7.91 ± 0.24 | 12.27 ± 0.36 | 11.63 ± 0.23 | 12.67 ± 0.21 | — | **13.38 ± 0.13** |
| 50 | 22.27 ± 0.24 | 21.13 ± 0.15 | — | **24.43 ± 0.38** | — | — |

关键发现：
- **Fixed `lambda=0.10` 在 100 类 IPC=10 上有害**（−5.2%），是最重要的动机性结果；
- **CAGS 在 IPC=10（+3.3%）和 IPC=50（+15.6%）上有效**，但在 IPC=1 上有害（−8.0%）；
- **IAST 在 IPC=1 上将损害从 −8.0% 缓解至 −4.9%**，标准差从 0.24 降至 0.06；
- **TAGS+CAGS 在 IPC=10 上达到 13.38%**，比 CAGS alone 高 0.71，比 unguided 高 1.11；
- **IPC=50 时 Random Herding 超过 unguided**（22.27 vs 21.13），但 CAGS 仍超过 Random Herding 2.16 个百分点。

### 6.3 跨架构评估（100 类，IPC=10）

| 架构 | Unguided | CAGS v2 | Δ 绝对 | Δ 相对 |
|------|----------:|---------:|-------:|-------:|
| ConvNet-6 | 12.27 ± 0.36 | 12.67 ± 0.21 | +0.40 | +3.3% |
| ResNet-18 | 13.87 ± 0.37 | **16.75 ± 0.29** | **+2.88** | **+20.8%** |
| ResNetAP-10 | 12.75 ± 0.16 | **15.29 ± 0.39** | **+2.54** | **+19.9%** |

CAGS 在所有三种架构上均有效，且在 ResNet-18 和 ResNetAP-10 上的增益远大于 ConvNet-6，证明改进不与特定架构耦合，且更深的网络能更好地利用 CAGS 生成的判别性特征。

### 6.4 ImageNette 与 ImageWoof（IPC=10）

| 数据集 | Random Herding | Unguided | CAGS v2 | CAGS 相对 unguided |
|---|---:|---:|---:|---:|
| ImageNette | 33.79 ± 0.67 | 38.99 ± 0.44 | **47.15 ± 0.44** | **+21.0%** |
| ImageWoof | 19.28 ± 0.30 | 22.00 ± 0.10 | **24.99 ± 0.05** | **+13.6%** |

CAGS 的收益不局限于 ImageNet-100 的类别划分。Unguided 相对 Random Herding 在两者上分别提高 5.20 和 2.72 个百分点，CAGS 相对 Random Herding 的总优势达到 13.36 和 5.71 个百分点。

### 6.5 TAGS 调度对比（100 类，IPC=10）

| 调度 | Top-1 | Top-5 | 标准差 |
|------|------:|------:|-------:|
| Linear TAGS+CAGS | **13.38** | 30.58 | **0.13** |
| Exponential TAGS+CAGS | 13.38 | **30.66** | 0.51 |

两种调度的均值完全相同，但 Linear 的方差明显更小，为默认配置。

### 6.6 FID

| 数据集 | IPC | Unguided | CAGS v2 | 变化（越低越好） |
|---|---:|---:|---:|---:|
| ImageNet-100（100 类） | 10 | 88.66 | **85.26** | −3.40 |
| ImageNet-100（100 类） | 50 | 71.06 | **59.28** | −11.78 |
| ImageNette | 10 | **105.21** | 108.59 | +3.38 |
| ImageWoof | 10 | **86.27** | 94.70 | +8.43 |

100 类任务中 CAGS 同时改善 FID 和准确率。ImageNette/Woof 出现 FID 变差但准确率提高的现象，说明 FID 与下游效用不严格一致，蒸馏方法应直接优化和评估下游效用。

### 6.7 100 类模块消融（IPC=10）

| 配置 | Top-1 (%) | Δ vs unguided |
|---|---:|---:|
| Unguided | 12.27 ± 0.36 | — |
| Fixed `lambda=0.10` | 11.63 ± 0.23 | −0.64 |
| CAGS v2 | 12.67 ± 0.21 | +0.40 |
| IAST+CAGS v2 (IPC=10，等同 CAGS) | 12.67 ± 0.21 | +0.40 |
| **Linear TAGS+CAGS v2** | **13.38 ± 0.13** | **+1.11** |

IAST 在 IPC=10 上因 range_scale=1.0 而完全透明，与 CAGS alone 等同。TAGS 提供时间维度的额外增益。

## 7. 尚未完成或需进一步验证的工作

- 外部 DD 方法（D4M、SRe²L、MTT、DMGD、CoDA 等）的统一协议比较表尚未完成；
- 统计显著性检验、95% 置信区间和效应量尚未计算；
- 类别级复杂度—lambda—准确率增益相关性分析尚未进行；
- 协议一致性核验（25/50 去噪步、224/256 分辨率、评估频率、归一化顺序）仍需完成；
- TAGS-only 在新协议下的完整评估尚未完成（当前仅有旧协议结果）；
- 10 类 IPC=1 的 IAST+CAGS 虽然有效，但是否有更优的缩放函数值得探索。

## 8. 对实验结果的总体分析

### 8.1 核心证据链

实验形成了一条清晰的逐级证据链：

1. **固定引导在类别异质性大时有害**：100 类 fixed `lambda=0.10` 下降 5.2%；
2. **CAGS 恢复并提升**：100 类 IPC=10 +3.3%，IPC=50 +15.6%；
3. **TAGS 在 CAGS 上继续增益**：100 类 IPC=10 达到 +9.0%，方差最低；
4. **IAST 保护低预算场景**：10 类 IPC=1 从 +11.8% 提升到 +18.1%，100 类 IPC=1 从 −8.0% 缓解到 −4.9%；
5. **CAGS 跨架构有效**：ResNet-18 +20.8%，ResNetAP-10 +19.9%，远大于 ConvNet-6 +3.3%。

### 8.2 CAGS 的适用边界

CAGS 在 IPC≥10 的所有设置中均有效，但在 100 类 IPC=1 上有害。这是因为低预算下每类只有一张合成图像，复杂度离散度大的类别获得强引导后该图像被过度锐化。IAST 通过缩小引导范围解决了这一问题。10 类 IPC=10 上最佳固定 `lambda=0.05` 比 CAGS 高 1.0 个百分点，说明类别较少时全局参数仍可能有效，这是 CAGS 的适用边界而非方法缺陷。

### 8.3 TAGS 与 IAST 的定位

TAGS 在与 CAGS 组合后展现明确增益，但 TAGS-only 在 10 类上未超过 unguided，说明时间适配需要类别校准为基础。IAST 的价值在低预算场景，IPC≥10 时完全透明。三模块不能笼统描述为"均独立有效"，而应定位为：CAGS 为核心贡献，TAGS 为时间维度增强，IAST 为预算保护机制。

### 8.4 FID 与准确率背离

CAGS 在 100 类上同时改善 FID 和准确率，但在 ImageNette/Woof 上 FID 变差而准确率提高。这说明 CAGS 优化的是训练数据效用，而非单纯的分布相似度。该定位符合数据集蒸馏目标。

### 8.5 机制解释的证据强度

"复杂类别需要更强引导"的解释与结果一致，并得到 normal CAGS 优于 inverted CAGS 的结果支持。但仍缺少类别级证据（每类复杂度、分配 lambda、每类准确率增益之间的相关性），目前应称其为"受实验支持的机制假设"。

## 9. 协议与论文表述风险

当前结果匹配 MGD³ 的主要下游训练超参数和最佳准确率跟踪，但"完全一致"的表述仍需核验：

1. 当前生成使用 25 个去噪步骤，MGD³ 原设置据项目记录为 50 步；
2. 论文写图像为 256×256，但项目记录指出当前数据或下游输入可能为 224×224；
3. 评估检查点频率（`epochs // 50` vs 100 个均匀检查点）需确认；
4. `quick_eval_v3_norm.py` 修正了归一化顺序并加入 Lighting augmentation，但尚未完整验证。

建议将表述改为"采用 MGD³-matched downstream optimization settings and best-accuracy tracking"。

## 10. 建议的论文主线

1. MGD³ 证明冻结扩散模型可用于训练自由数据集蒸馏，但固定 CFG 假设过强；
2. 10 类上适度固定引导可能有效，说明 CFG 本身具有价值；
3. 类别增至 100 后，全局引导出现负迁移，暴露统一参数的局限；
4. CAGS 根据类别复杂度分配引导，在 100 类上从负增益转为正增益，并在多个数据集和 IPC 上持续超过 unguided；
5. TAGS 在 CAGS 上提供时间维度增益，IAST 保护低预算场景；
6. 跨架构验证证明 CAGS 改进不与特定架构耦合；
7. FID 与准确率并非总是一致，蒸馏方法应直接优化下游效用。

## 11. 下一步优先级

1. 核对 25/50 去噪步、224/256 分辨率、评估频率和归一化顺序；
2. 按统一协议整理最新 DD 方法比较表；
3. 对主要方法执行配对显著性检验并报告 95% 置信区间与效应量；
4. 增加类别级复杂度—lambda—准确率增益相关性分析；
5. 补充新协议下的 TAGS-only 评估；
6. 探索 IAST 缩放函数的替代设计（如线性、指数而非对数）。

## 12. 当前审阅结论

全部实验已完成，三模块叙事得到完整实验支撑。CAGS 识别并缓解了固定 CFG 在类别异质性条件下的失效，是核心贡献；TAGS 在 CAGS 上提供时间维度增益且方差最低；IAST 通过引导范围缩放保护低预算场景，在 10 类上提供额外增益并在 100 类上缓解损害。跨架构验证证明改进不与 ConvNet-6 耦合，ResNet-18 和 ResNetAP-10 上的增益远大于 ConvNet-6。Random Herding 在所有设置中被 unguided 和 CAGS 超越，确认了扩散先验的价值。FID 与准确率的背离进一步支持"蒸馏方法应以下游效用为核心指标"的定位。剩余工作主要是协议核验、统计检验、外部方法比较表和机制分析，当前思路已具备形成完整投稿的实验基础。
