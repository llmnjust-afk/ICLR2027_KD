# AGS-DD 当前研究思路、创新与实验结果审阅稿

> 文档状态：阶段性总结（更新于 2026-09-03 03:52 UTC）  
> 目标投稿：ICLR 2027  
> 工作名称：Adaptive Guidance Scheduling for Training-Free Diffusion Dataset Distillation（AGS-DD）

## 1. 一句话概括

本工作以 MGD³ 的训练自由扩散数据集蒸馏流程为核心 baseline，研究如何将其“所有类别、所有去噪时刻和所有 IPC 预算共用同一引导配置”的做法，改造成类别自适应、时间步自适应和预算自适应的生成策略。当前结果已支持两个主要模块：CAGS 根据类别复杂度分配不同的 classifier-free guidance（CFG）强度，TAGS 则沿去噪时间步调节 CAGS 给出的引导。固定 CFG 在类别异质性较强的 100 类任务上降低下游准确率，CAGS 能够逆转这一退化，而 linear TAGS+CAGS 进一步取得当前最佳的 `13.38 ± 0.13%`。IAST+CAGS 在 IPC=10 上未超过 CAGS alone，因此当前最准确的定位是“以 CAGS 为核心贡献，以 TAGS 为得到组合增益验证的时间扩展，以 IAST 为效果依赖 IPC、仍需多预算验证的扩展”。

## 2. 研究问题与核心假设

### 2.1 研究问题

MGD³ 使用冻结的预训练 DiT 直接生成类别条件合成图像，不需要优化生成模型或合成图像，因此具有训练自由、单次生成和易扩展的优势。然而，其生成阶段使用全局固定的 CFG 强度，并采用固定的高噪声生成窗口。这种统一配置忽略了三个差异：

1. 不同类别的类内变化、类间可分性和视觉模态数不同；
2. CFG 在不同扩散时间步上的作用不同；
3. IPC=1 与 IPC=50 对单张样本代表性和集合多样性的需求不同。

### 2.2 核心假设

本工作的核心假设是：数据集蒸馏所需的最优引导并非常数，而是类别、时间步和 IPC 预算的函数。固定引导可能在简单类别上过度锐化并损失多样性，同时在复杂类别上引导不足；这种冲突会随着类别数量和类别异质性的增加而更加明显。若能针对这些差异自适应地控制生成过程，就能在不训练 DiT、不增加可学习参数的前提下，提高合成数据训练下游分类器的有效性。

## 3. Baseline 与比较对象

### 3.1 核心方法 baseline：MGD³

本工作的直接方法 baseline 是 MGD³ 的训练自由扩散数据集蒸馏范式。其基本流程为：

1. 使用在 ImageNet-1K 上预训练的冻结 DiT-XL/2 作为生成器；
2. 通过类别条件和高噪声起点执行反向扩散；
3. 生成固定 IPC 数量的合成图像；
4. 仅使用合成图像训练下游分类器；
5. 以分类器测试集最佳 Top-1 准确率衡量蒸馏数据质量。

本文不改变 DiT 参数，也不引入生成模型微调，而是在 MGD³ 的采样阶段修改引导策略。因此，最直接、最公平的内部对照包括：

- **Unguided**：`lambda = 0`，即无 CFG 的 MGD³ 生成；
- **Fixed lambda**：所有类别与时间步使用同一个 CFG 强度，是本文要突破的主要对象；
- **CAGS v1**：早期类别自适应版本，用于说明复杂度建模的演进；
- **CAGS v2**：当前主要方法，使用更完整的多因素复杂度指标。

### 3.2 数据选择 baseline：Random Herding

Random Herding 从真实训练集中按类别选取给定 IPC 的图像，用于回答“直接保留少量真实图像”与“由冻结扩散模型生成合成图像”之间的差异。污染进程清理后，10 类 IPC=1/10/50 已通过顺序执行和独立日志完成重跑，结果分别为 `14.67 ± 0.66%`、`34.07 ± 0.90%` 和 `48.33 ± 0.50%`。ImageNette 和 ImageWoof 的 IPC=10 结果分别为 `33.79 ± 0.67%` 和 `19.28 ± 0.30%`。在这些设置中，unguided MGD³ 均高于 Random Herding，CAGS 又进一步高于 unguided，说明冻结扩散先验相对随机真实子集具有一致优势，而自适应引导能继续改善合成数据效用。100 类 Random Herding 的 IPC=1/10/50 评估已于 GPU1 排队，其中 IPC=10 正在运行，尚无完整 3-seed 结果。

### 3.3 外部数据集蒸馏方法

论文计划与 D4M、SRe²L、MTT、DMGD、CoDA、IDC-1 和 MinMaxDiff 等已发表方法比较。当前正文仅列出部分方法名称，尚未形成经过统一数据集、IPC、架构和训练协议核对的完整对比表。因此，这些方法目前只能视为计划中的外部参照，不能视为已经完成的实验结论。

## 4. 方法创新

## 4.1 CAGS：类别自适应引导强度

CAGS 是当前工作最核心、证据最充分的创新。它先从真实训练图像提取 VAE 特征，再为每个类别估计复杂度，最后把归一化复杂度映射为类别专属 CFG 强度：

```text
lambda_c = lambda_min + (lambda_max - lambda_min) * rho_c
```

其中 `rho_c` 综合考虑以下信息：

- 类内方差：刻画同一类别内部视觉变化；
- 类间可分性：刻画该类别与最近其他类别的混淆程度；
- 结构多样性：通过特征聚类刻画类别包含的视觉模态；
- 聚类或分散度相关统计：用于补充类别结构复杂度。

CAGS v2 当前采用的权重为 `(0.15, 0.15, 0.35, 0.35)`，并通过 sigmoid 将复杂度压缩至稳定区间。复杂类别获得更强引导，以突出判别特征；简单类别获得较弱引导，以避免过度锐化和多样性损失。复杂度只需离线计算一次并缓存，因此不会引入模型训练，也不会显著增加后续采样开销。

与“搜索一个更好的全局 lambda”相比，CAGS 的实质创新是把 CFG 从数据集级超参数提升为类别级控制变量。100 类实验提供了最直接证据：固定 `lambda=0.10` 低于无引导，而 CAGS 在相同生成框架下超过无引导，说明改进并非仅来自“使用了 CFG”，而来自“针对类别异质性分配 CFG”。

## 4.2 TAGS：时间步自适应引导调度

TAGS 将常数引导改为随扩散时间步变化的函数，当前考察 cosine/annealed、linear 和 exponential 三类调度。其动机是：高噪声阶段主要确定全局结构，低噪声阶段主要修正局部细节，因此同一引导强度未必适合整个去噪轨迹。

现有 TAGS-only 数字来自旧协议（学习率 0.05、报告最终 epoch），不可与当前主结果直接比较。在该旧协议下，所有 TAGS-only 配置均未超过 unguided；较低峰值 `lambda=0.05` 优于 `lambda=0.10`，其中 exponential 为较强配置。新协议下，100 类 IPC=10 的 linear TAGS+CAGS 达到 `13.38 ± 0.13%`，比 CAGS alone 高 0.71 个百分点，比 unguided 高 1.11 个百分点，是当前该任务上的最佳配置。三个种子分别为 13.20%、13.48% 和 13.46%，说明组合增益具有较好的一致性。该结果支持 TAGS 与 CAGS 的互补性，但尚不能证明 TAGS 的独立贡献，因为缺少同一新协议下的 TAGS-only 对照；exponential TAGS+CAGS 已在 GPU1 排队，用于比较时间调度形式。

## 4.3 IAST：IPC 自适应起始时间步

IAST 根据 IPC 调整反向扩散的高噪声起点。其设计假设是：IPC 较小时，每张图像都应更典型、更具代表性，因此采用较低噪声起点；IPC 较大时，样本集合可以承担更高变化，因而采用较高噪声起点以增加多样性。当前使用对数形式在 IPC 范围内插值起始时间步。

新协议下，100 类 IPC=10 的 IAST+CAGS 达到 `12.57 ± 0.15%`，比 unguided 高 0.30 个百分点，但比 CAGS alone 低 0.10 个百分点。该结果表明 IAST 在 IPC=10 上没有提供额外增益，可能是因为固定的 `t_start=25` 已接近该预算的适宜起点。由于 IAST 的设计目标本身是随 IPC 改变起点，其有效性必须结合 IPC=1 和 IPC=50 评估；仅凭 IPC=10 不能否定该设计，也不能声称其已经有效。

## 4.4 统一 AGS 采样器

完整 AGS 将三个控制维度组合起来：CAGS 决定类别级基础引导，TAGS 决定该引导沿时间步如何变化，IAST 决定从哪个噪声位置开始生成。整个过程使用冻结 DiT，不计算梯度、不更新权重，也不引入可训练参数。相较训练型样本选择或生成优化方法，其预期优势是保持 MGD³ 的单次生成和低额外成本。

## 5. 当前实验协议

### 5.1 已采用的下游评估设置

- 优化器：SGD；
- 初始学习率：0.1；
- 权重衰减：`1e-4`；
- batch size：128；
- 所有下游架构目标上均使用 instance normalization；
- 训练过程中记录最佳 Top-1，而不是只报告最终 epoch；
- 每个正式配置使用 3 个随机种子 `0, 1, 2`；
- 10 类 IPC=1/10/50 分别训练 3000/2000/1500 epochs；
- 100 类 IPC=1/10/50 分别训练 2000/1300/1000 epochs。

### 5.2 实验范围

计划范围为 4 个数据集、3 个 IPC 和 3 个下游架构：

- 数据集：ImageNet-100 的 10 类子集、完整 ImageNet-100、ImageNette、ImageWoof；
- IPC：1、10、50；
- 架构：ConvNet-6、ResNet-18、ResNetAP-10；
- 额外分析：CAGS/TAGS/IAST 消融及 FID。

当前已完成结果主要集中在 ConvNet-6；因此“4×3×3 全范围验证”尚未完成。

## 6. 当前已完成的正式结果

除特别说明外，下表均为 ConvNet-6、instance normalization、3 个种子和训练过程最佳 Top-1。

### 6.1 ImageNet-100 的 10 类子集

| IPC | 方法 | Top-1（%） | 相对 unguided 变化 |
|---:|---|---:|---:|
| 1 | Random Herding | 14.67 ± 0.66 | −23.6% |
| 1 | Unguided | 19.20 ± 0.49 | — |
| 1 | CAGS v2 | **21.47 ± 1.67** | **+11.8%** |
| 10 | Random Herding | 34.07 ± 0.90 | −12.8% |
| 10 | Unguided | 39.07 ± 1.16 | — |
| 10 | Fixed `lambda=0.05` | **41.87 ± 0.25** | **+7.2%** |
| 10 | CAGS v2 | 40.87 ± 0.98 | +4.6% |
| 50 | Random Herding | 48.33 ± 0.50 | −1.0% |
| 50 | Unguided | 48.80 ± 0.82 | — |
| 50 | CAGS v2 | **53.47 ± 0.82** | **+9.6%** |

结果说明 CAGS 在三个 IPC 上均超过 unguided，表明类别自适应策略并不只适用于单一预算。但在 10 类 IPC=10 上，最佳固定 `lambda=0.05` 比 CAGS 高 1.00 个百分点。因此，当前证据不支持“CAGS 在所有设置下优于最优固定引导”，更准确的结论是：CAGS 相对 unguided 具有跨 IPC 一致性，并且在类别异质性较强时比固定引导更有优势。

IPC=1 的相对增益最大，但其标准差从 0.49 增至 1.67，说明平均性能提升伴随更大的种子波动。IPC=50 获得 4.67 个百分点的最大绝对增益，支持“更充足的每类样本有利于复杂度估计或多样性发挥”的解释，但仍需通过重复复杂度估计或置信区间分析验证具体机制。Random Herding 与 unguided 的差距随 IPC 增加而明显收窄：IPC=1/10/50 分别相差 4.53、5.00 和 0.47 个百分点，而 CAGS 在 IPC=50 重新把优势扩大至 5.14 个百分点。这说明高 IPC 时随机真实子集已具有较强竞争力，CAGS 的主要价值在于利用生成先验和类别自适应引导获得超越简单样本保留的额外收益。

### 6.2 完整 ImageNet-100，IPC=10

| 方法 | Top-1（%） | 绝对变化 | 相对 unguided 变化 |
|---|---:|---:|---:|
| Unguided | 12.27 ± 0.36 | — | — |
| Fixed `lambda=0.10` | 11.63 ± 0.23 | −0.64 | −5.2% |
| CAGS v2 `[0.0, 0.06]` | 12.67 ± 0.21 | +0.40 | +3.3% |
| IAST + CAGS v2 | 12.57 ± 0.15 | +0.30 | +2.4% |
| **Linear TAGS + CAGS v2** | **13.38 ± 0.13** | **+1.11** | **+9.0%** |

CAGS 的三个种子分别为 12.96%、12.50% 和 12.54%，均值高于 unguided；其标准差 0.21 也小于 unguided 的 0.36。相对 fixed `lambda=0.10`，CAGS 提升 1.04 个百分点，即约 8.9%。Linear TAGS+CAGS 又在 CAGS 的基础上提升 0.71 个百分点，并将标准差降至 0.13，说明类别维度与时间维度的适配可以互补。IAST+CAGS 则略低于 CAGS alone，因此三模块不能笼统描述为“均提供独立增益”。

这些结论仍有边界。第一，目前只比较了 100 类下的一个固定强度 `lambda=0.10`，尚不能证明所有可能的固定 lambda 都低于 CAGS；应补充较小 lambda 的 sweep 或明确将结论限定为“所评估的固定配置”。第二，TAGS 的新协议证据来自 TAGS+CAGS 组合，缺少 TAGS-only 对照，因而只能证明组合有效，尚不能完全隔离 TAGS 的独立贡献。第三，当前只有均值和标准差，尚未报告配对检验、置信区间或效应量，因此不能声称统计显著。

### 6.3 ImageNette 与 ImageWoof，IPC=10

| 数据集 | Random Herding（%） | Unguided（%） | CAGS v2（%） | CAGS 相对 unguided 提升 |
|---|---:|---:|---:|---:|
| ImageNette | 33.79 ± 0.67 | 38.99 ± 0.44 | **47.15 ± 0.44** | +21.0% |
| ImageWoof | 19.28 ± 0.30 | 22.00 ± 0.10 | **24.99 ± 0.05** | +13.6% |

这两组结果说明 CAGS 的收益不局限于 ImageNet-100 的特定类别划分。ImageNette 上取得当前最大的相对增益，而在细粒度犬种分类 ImageWoof 上仍有稳定提升，初步表明方法同时适用于较容易的类别区分和较高的类间相似性。Unguided 相对 Random Herding 在两者上分别提高 5.20 和 2.72 个百分点，CAGS 相对 Random Herding 的总优势则达到 13.36 和 5.71 个百分点。不过，目前缺少对应的最佳 fixed lambda 对照，因此还不能把提升完全归因于“类别自适应优于任何固定引导”，只能确认其优于 unguided 和 Random Herding。

### 6.4 FID

| 数据集 | IPC | Unguided | CAGS v2 | 变化（越低越好） |
|---|---:|---:|---:|---:|
| ImageNet-100（100 类） | 10 | 88.66 | **85.26** | −3.40 |
| ImageNet-100（100 类） | 50 | 71.06 | **59.28** | −11.78 |
| ImageNette | 10 | **105.21** | 108.59 | +3.38 |
| ImageWoof | 10 | **86.27** | 94.70 | +8.43 |

100 类任务中，CAGS 同时改善 FID 和分类准确率，尤其在 IPC=50 上 FID 降低 11.78。ImageNette 和 ImageWoof 则出现 FID 变差但分类准确率提高的现象。这说明 FID 与蒸馏数据的下游效用并不严格一致：CAGS 可能强化了分类相关特征，却使整体生成分布在 Inception 特征空间中偏离真实验证集。该结果支持“数据集蒸馏必须以下游训练效果为核心指标”，但不能仅凭 FID/准确率背离就断言具体因果机制，后续可增加类条件 FID、precision/recall、特征可分性和类内多样性分析。

## 7. 尚未完成或不可作为正式结论的实验

- 100 类 exponential TAGS+CAGS 已排队，用于与已完成的 linear TAGS+CAGS 比较；
- 新协议 TAGS-only 尚未评估，因此 TAGS 的独立贡献仍未隔离；
- 100 类 IPC=1、IPC=50 的 unguided 与 CAGS 尚待完成；
- ResNet-18 和 ResNetAP-10 的跨架构结果尚待完成；
- 100 类 Random Herding IPC=1/10/50 已在 GPU1 顺序运行，其中 IPC=10 正在进行；
- TAGS-only 与 CAGS 方向性消融目前来自旧协议，只能作为探索性结果；
- 外部 DD 方法的统一比较表尚未完成；
- 统计显著性、95% 置信区间和效应量尚未计算。

因此，论文可以陈述“linear TAGS+CAGS 在 100 类 IPC=10 上进一步提升 CAGS”，但不能陈述“每个模块均独立贡献”：IAST+CAGS 未超过 CAGS alone，TAGS-only 也尚未在新协议下完成。“CAGS 跨所有架构有效”仍属于待验证陈述。

## 8. 对当前实验结果的总体分析

### 8.1 当前最强结论

最可靠的结论不是“自适应方法在所有表格中都取得最高准确率”，而是“固定引导的作用具有数据集和类别规模依赖性，而类别与时间步的联合适配能提高跨设置稳健性”。10 类 IPC=10 上固定小 lambda 最好，说明类别较少且异质性有限时，全局参数仍可能有效；100 类上 fixed `lambda=0.10` 反而有害，CAGS 超过 unguided，linear TAGS+CAGS 又进一步提高至 13.38%。这一逐级对照形成了清晰证据链：固定全局引导失效，类别适配恢复收益，时间适配在类别校准基础上继续增益。

### 8.2 TAGS 与 IAST 的新证据

Linear TAGS+CAGS 比 CAGS alone 提高 0.71 个百分点，且三个种子结果集中，表明沿时间步调节已校准的类别引导是有效方向。不过，当前实验不能区分增益究竟来自 linear 调度本身，还是特定调度与 CAGS 范围的交互；exponential 组合和 TAGS-only 对照对此至关重要。IAST+CAGS 比 CAGS alone 低 0.10 个百分点，说明 IPC=10 上改变起始时间步没有收益，也提醒完整 AGS 不应默认叠加全部模块；IAST 是否应保留为核心模块，取决于 IPC=1/50 的结果。

### 8.3 CAGS 的优势边界

CAGS 已在 4 个主要设置族中超过 unguided：10 类的 3 个 IPC、100 类 IPC=10、ImageNette IPC=10 和 ImageWoof IPC=10。其优势表现出较好的数据集覆盖，但尚未覆盖完整的 4×3×3 实验矩阵。尤其是跨架构结果决定改进究竟来自合成数据本身，还是与 ConvNet-6 的归纳偏置耦合，因此是当前优先级最高的缺口之一。

### 8.4 机制解释的证据强度

当前关于“复杂类别需要更强引导、简单类别需要更弱引导”的解释与结果一致，并得到 normal CAGS 优于 inverted CAGS 的旧协议结果支持。然而，尚缺少类别级证据，例如每类复杂度、分配 lambda、每类准确率增益之间的相关性，以及调整单个复杂度因子的消融。因此，目前应称其为“受实验支持的机制假设”，而非已经严格证明的因果机制。

### 8.5 FID 与准确率背离的意义

FID 结果使论文不必把贡献局限为“生成更逼真的图像”。更合理的定位是：AGS 优化的是训练数据效用，而非单纯的生成分布相似度。该定位符合数据集蒸馏目标，也能解释为什么 ImageNette/Woof 的准确率提高而 FID 变差。不过，需要谨慎避免把这种背离自动解释为“判别特征被锐化”；应通过特征空间可视化、线性探针或类内/类间距离分析提供直接证据。

## 9. 协议与论文表述风险

当前结果已经匹配 MGD³ 的主要下游训练超参数和最佳准确率跟踪思想，但“完全一致”或“exact MGD³ protocol”的表述仍需核验：

1. 当前生成使用 25 个去噪步骤，而 MGD³ 原设置据项目记录为 50 步；
2. 论文写生成/训练图像为 256×256，但已有项目记录指出当前数据或下游输入可能为 224×224；
3. 当前已完成进程可能按 `epochs // 50` 评估，而新脚本计划使用 100 个均匀检查点；
4. `quick_eval_v3_norm.py` 修正了归一化顺序并加入 Lighting augmentation，但尚未完整验证；
5. 当前论文将较低绝对准确率部分归因于方法差异，但生成步数和分辨率差异也可能是重要因素。

在这些项目完成核验前，建议将表述改为“采用 MGD³-matched downstream optimization settings and best-accuracy tracking”，而不是声称完整复现全部生成与评估细节。

## 10. 建议的论文主线

建议将论文论证顺序收敛为：

1. MGD³ 证明冻结扩散模型可用于训练自由数据集蒸馏，但固定 CFG 假设过强；
2. 10 类上适度固定引导可能有效，说明 CFG 本身具有价值；
3. 类别增至 100 后，相同形式的全局引导出现负迁移，暴露统一参数的局限；
4. CAGS 根据类别复杂度分配引导，在 100 类上从负增益转为正增益，并在多个数据集和 IPC 上持续超过 unguided；
5. Linear TAGS 在 CAGS 上提供额外收益，支持类别与时间联合适配；IAST 在 IPC=10 上没有额外收益，需由 IPC=1/50 决定其定位；
6. FID 与准确率并非总是一致，进一步说明蒸馏方法应直接优化和评估下游效用。

## 11. 下一步优先级

1. 完成 100 类 IPC=1/50 的 unguided 与 CAGS，验证 CAGS 在高异质性任务上的预算趋势；
2. 完成 ResNet-18、ResNetAP-10，以验证合成数据的架构迁移性；
3. 完成 exponential TAGS+CAGS，并补充新协议 TAGS-only，以隔离时间调度贡献；
4. 完成 100 类 Random Herding IPC=1/10/50，补齐真实子集基线；
5. 核对 25/50 去噪步、224/256 分辨率、评估频率和归一化顺序；
6. 对主要三种方法执行配对显著性检验并报告 95% 置信区间与效应量；
7. 增加类别级复杂度—lambda—准确率增益相关性分析，以强化机制证据；
8. 按统一协议整理最新 DD 方法比较，避免混用不同架构或训练设置的公开数字。

## 12. 当前审阅结论

当前工作已经形成两层清晰且有实验支撑的贡献。第一，CAGS 识别并缓解了固定 CFG 在类别异质性条件下的失效；100 类 IPC=10 的“fixed 下降、CAGS 上升”是核心结果，10 类多 IPC、ImageNette 和 ImageWoof 提供外部一致性。第二，linear TAGS+CAGS 将准确率进一步提高到 `13.38 ± 0.13%`，表明时间步适配与类别适配具有互补性。Random Herding 的完整 10 类、ImageNette 和 ImageWoof 结果还确认了扩散合成相对随机真实子集的一致优势。IAST 在 IPC=10 上没有额外收益，因此不宜把三个模块包装为同等强度的贡献；其最终定位应等待多 IPC 结果。补齐跨架构、100 类多 IPC、TAGS-only、协议一致性和统计检验后，当前工作具备形成完整投稿的潜力。
