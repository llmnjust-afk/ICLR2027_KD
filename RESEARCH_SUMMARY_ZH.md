# AGS-DD 当前研究思路、创新与实验结果审阅稿

> 文档状态：协议修正后完整更新（2026-09-04 UTC，全部实验已完成）  
> 目标投稿：ICLR 2027  
> 工作名称：Adaptive Guidance Scheduling for Training-Free Diffusion Dataset Distillation（AGS-DD）

## 1. 一句话概括

本工作以 MGD³ 的训练自由扩散数据集蒸馏流程为核心 baseline，将"所有类别共用同一引导配置"的做法改造成类别自适应（CAGS）、时间步自适应（TAGS）和预算自适应（IAST）的生成策略。全部实验已在修正后的 MGD³ 匹配协议下完成（新增 Lighting 增强、修正归一化顺序、50 去噪步）：4 个数据集 × 3 个 IPC × 3 个架构、完整模块消融、FID 分析和 Random Herding 基线。CAGS 在所有数据集和所有 IPC≥1 的设置中均超过 unguided，增益范围 +1.6%~+19.4%；CAGS 在 10 类 IPC=10 上也超过最佳固定 λ（50.13% vs 48.47%）；跨架构增益显著（ResNet-18 +13.8%，ResNetAP-10 +15.2%）。IAST 在 100 类 IPC=1 上发挥关键保护作用（+11.2%），但在 10 类 IPC=1 上有害（−13.0%），揭示了重要的适用边界。TAGS 在修正协议下不提供额外增益，确认类别轴是主要变异轴。

## 2. 研究问题与核心假设

### 2.1 研究问题

MGD³ 使用冻结的预训练 DiT 直接生成类别条件合成图像，不需要优化生成模型或合成图像，因此具有训练自由、单次生成和易扩展的优势。然而，其生成阶段使用全局固定的 CFG 强度，忽略了不同类别的类内变化、类间可分性和视觉模态数差异。这种统一配置在类别异质性大时会导致简单类别被过度引导而复杂类别引导不足。

### 2.2 核心假设

本工作的核心假设是：数据集蒸馏所需的最优引导并非常数，而是类别、时间步和 IPC 预算的函数。固定引导可能在简单类别上过度锐化并损失多样性，同时在复杂类别上引导不足；这种冲突会随着类别数量和类别异质性的增加而更加明显。若能针对这些差异自适应地控制生成过程，就能在不训练 DiT、不增加可学习参数的前提下，提高合成数据训练下游分类器的有效性。

## 3. Baseline 与比较对象

### 3.1 核心方法 baseline：MGD³

本工作的直接方法 baseline 是 MGD³ 的训练自由扩散数据集蒸馏范式。本文不改变 DiT 参数，而是在 MGD³ 的采样阶段修改引导策略。内部对照包括：

- **Unguided**：`lambda = 0`，即无 CFG 的 MGD³ 生成；
- **Fixed lambda**：所有类别与时间步使用同一个 CFG 强度；
- **CAGS v2**：当前主要方法，权重 `(0.15, 0.15, 0.35, 0.35)`，sigmoid 参数 `slope=5.0, center=0.5`。

### 3.2 数据选择 baseline：Random Herding

Random Herding 从真实训练集中按类别选取给定 IPC 的图像，用于回答"直接保留少量真实图像"与"由冻结扩散模型生成合成图像"之间的差异。

### 3.3 外部数据集蒸馏方法

论文计划与 D4M、SRe²L、MTT、DMGD、CoDA、IDC-1 和 MinMaxDiff 等已发表方法比较。当前正文仅列出部分方法名称，尚未形成经过统一协议核对的完整对比表，仍为计划中的外部参照。

## 4. 方法创新

### 4.1 CAGS：类别自适应引导强度

CAGS 先从真实训练图像提取 VAE 特征，再为每个类别估计复杂度，最后把归一化复杂度映射为类别专属 CFG 强度。复杂度指标综合类内方差、类间可分性、结构多样性和聚类分散度。复杂类别获得更强引导以突出判别特征；简单类别获得较弱引导以避免过度锐化。复杂度只需离线计算一次并缓存，不引入模型训练。

核心创新是把 CFG 从数据集级超参数提升为类别级控制变量。100 类实验提供了最直接证据：固定 `lambda=0.10` 略低于无引导（−1.9%），而 CAGS 在相同生成框架下超过无引导 +7.4%。在 10 类实验中，CAGS 也超过最佳固定 `lambda=0.05`（50.13% vs 48.47%），证明即使类别数少、异质性有限，逐类适配仍优于全局调参。

### 4.2 TAGS：时间步自适应引导调度

TAGS 将常数引导改为随扩散时间步变化的函数，考察 cosine/annealed、linear 和 exponential 三类调度。在修正协议下，100 类 IPC=10 的 linear TAGS+CAGS 达到 `20.31 ± 0.44%`，仍优于 unguided（+3.3%），但低于 CAGS alone（21.11%）。这表明在类别轴适配到位后，时间轴调制不提供额外增益。该负面结果具有科学价值：它确认了固定引导的主要缺陷来源是逐类引导失配，而非引导的时间分布。

### 4.3 IAST：IPC 自适应引导范围

IAST 根据 IPC 缩放 CAGS 的引导范围：

```text
range_scale(IPC) = min(1.0, log(IPC+1) / log(IPC_ref+1))   # IPC_ref = 10
[lambda_min_eff, lambda_max_eff] = range_scale * [lambda_min, lambda_max]
```

各 IPC 下的效果：

| IPC | range_scale | 100类 [0, 0.06] → 实际范围 | 10类 [0, 0.08] → 实际范围 |
|---:|---:|---:|---:|
| 1 | 0.289 | [0, 0.017] | [0, 0.023] |
| 10 | 1.000 | [0, 0.06]（不变） | [0, 0.08]（不变） |
| 50 | 1.000 | [0, 0.06]（不变） | [0, 0.08]（不变） |

修正协议下的实验验证：
- **100 类 IPC=1**：IAST+CAGS `5.66 ± 0.20%`，是所有方法中最佳（+11.2% vs unguided），比 CAGS alone `5.17 ± 0.23%` 高 0.49 个百分点（+9.5%），标准差从 0.23 降至 0.20；
- **10 类 IPC=1**：IAST+CAGS `20.87 ± 0.62%`，低于 unguided `24.00 ± 0.28%`（−13.0%），也低于 CAGS alone `24.53 ± 0.68%`（−14.9%）。

这一对比揭示了重要的适用边界：IAST 的对数缩放在类别数多（100 类）、复杂度变异大时有效，因为缩放后的范围仍保留了足够的逐类差异；但在类别数少（10 类）、复杂度变异有限时，同样的缩放将范围压缩至 [0, 0.023]，使逐类引导差异低于噪声水平，反而有害。未来工作应将缩放函数条件化于类别数，而不仅仅依赖 IPC。

### 4.4 统一 AGS 采样器

完整 AGS 将三个控制维度组合：IAST 缩放 CAGS 引导范围，CAGS 在缩放后的范围内决定类别级基础引导，TAGS 决定该引导沿时间步如何变化。整个过程使用冻结 DiT，不计算梯度、不更新权重。

## 5. 当前实验协议

### 5.1 生成设置

- 预训练 DiT-XL/2，冻结，不训练；
- 50 个去噪步骤，256×256 分辨率；
- 高噪声引导窗口：前 25 步施加 CFG；
- VAE：`stabilityai/sd-vae-ft-mse`。

### 5.2 下游评估设置（MGD³ 匹配）

- 优化器：SGD，初始学习率 0.1，权重衰减 `1e-4`，batch size 128；
- MultiStepLR，γ=0.2，在 2/3 和 5/6 epochs 处衰减；
- 所有下游架构均使用 instance normalization；
- 数据增强：RandomResizedCrop(224, scale=(0.5,1.0)) → HFlip → ColorJitter(0.4,0.4,0.4) → Lighting(alphastd=0.1) → Normalize → CutMix(p=1.0, β=1.0)；
- 归一化在增强之后进行（修正了旧协议中先归一化再增强的错误）；
- 训练过程中记录最佳 Top-1（不是最终 epoch）；
- 每个配置使用 3 个随机种子 `0, 1, 2`；
- 10 类 IPC=1/10/50 分别训练 3000/2000/1500 epochs；
- 100 类 IPC=1/10/50 分别训练 2000/1300/1000 epochs。

### 5.3 协议修正说明

旧协议存在两个问题：(1) 缺少 Lighting 增强；(2) 归一化顺序错误（先归一化再增强）。修正后绝对准确率大幅提升（例如 100 类 IPC=10 unguided 从 12.27% 升至 19.66%），与 MGD³ 发表值量级一致。生成端（50 步、256×256）无需修正。

## 6. 全部已完成正式结果

除特别说明外，下表均为 ConvNet-6、instance normalization、3 个种子和训练过程最佳 Top-1。

### 6.1 ImageNet-100 的 10 类子集

| IPC | Random Herding | Unguided | Fixed `lambda=0.05` | CAGS v2 | IAST+CAGS v2 |
|---:|---:|---:|---:|---:|---:|
| 1 | 16.00 ± 0.43 | 24.00 ± 0.28 | — | 24.53 ± 0.68 | 20.87 ± 0.62 |
| 10 | 39.60 ± 1.42 | 45.40 ± 0.75 | 48.47 ± 0.25 | **50.13 ± 0.74** | — |
| 50 | 64.73 ± 0.34 | 58.93 ± 0.94 | — | **68.07 ± 1.27** | — |

关键发现：
- **CAGS 在所有三个 IPC 上均超过 unguided**，增益随 IPC 单调递增：+2.2% → +10.4% → +15.5%；
- **CAGS 超过最佳固定 `lambda=0.05`**（50.13% vs 48.47%），证明即使类别少、异质性有限，逐类适配仍优于全局调参；
- **IAST+CAGS 在 10 类 IPC=1 上有害**（−13.0%），因为对数缩放将范围从 [0, 0.08] 压缩至 [0, 0.023]，在复杂度变异有限时去除了自适应信号；
- **Random Herding 在 IPC=50 上超过 unguided**（64.73 vs 58.93），因为高预算时真实图像的多样性优势显现；但 CAGS 仍超过 RH 3.34 个百分点。

### 6.2 完整 ImageNet-100

| IPC | Random Herding | Unguided | Fixed `lambda=0.10` | CAGS v2 | IAST+CAGS v2 | TAGS(linear)+CAGS |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 3.81 ± 0.22 | 5.09 ± 0.05 | — | 5.17 ± 0.23 | **5.66 ± 0.20** | — |
| 10 | 13.67 ± 0.69 | 19.66 ± 0.18 | 19.29 ± 0.10 | **21.11 ± 0.50** | — | 20.31 ± 0.44 |
| 50 | 35.21 ± 0.15 | 31.52 ± 0.15 | — | **37.63 ± 0.32** | — | — |

关键发现：
- **Fixed `lambda=0.10` 在 100 类 IPC=10 上仍有害**（−1.9%），虽然幅度小于旧协议（−5.2%），但方向一致，确认了固定引导在类别异质性大时的固有缺陷；
- **CAGS 在 IPC=10（+7.4%）和 IPC=50（+19.4%）上显著有效**，IPC=50 的 +19.4% 是全研究最大增益；
- **IAST 在 IPC=1 上是最佳方法**（+11.2% vs unguided），比 CAGS alone（+1.6%）高 9.6 个百分点相对，证明范围缩放在高类别数低预算时至关重要；
- **TAGS+CAGS 低于 CAGS alone**（20.31 vs 21.11），时间轴调制在类别轴适配到位后不提供额外增益；
- **IPC=50 时 Random Herding 超过 unguided**（35.21 vs 31.52，+11.7%），但 CAGS 仍超过 RH 2.42 个百分点（+6.9%）。

### 6.3 跨架构评估（100 类，IPC=10）

| 架构 | Unguided | CAGS v2 | Δ 绝对 | Δ 相对 |
|------|----------:|---------:|-------:|-------:|
| ConvNet-6 | 19.66 ± 0.18 | 21.11 ± 0.50 | +1.45 | +7.4% |
| ResNet-18 | 22.92 ± 0.13 | **26.09 ± 0.04** | **+3.17** | **+13.8%** |
| ResNetAP-10 | 22.88 ± 0.31 | **26.36 ± 0.46** | **+3.48** | **+15.2%** |

CAGS 在所有三种架构上均有效，且在 ResNet-18 和 ResNetAP-10 上的增益远大于 ConvNet-6。ResNet-18 的标准差仅 ±0.04%，是全研究中最稳定的配置。更深的网络能更好地利用 CAGS 生成的判别性特征。

### 6.4 ImageNette 与 ImageWoof（IPC=10）

| 数据集 | Random Herding | Unguided | CAGS v2 | CAGS 相对 unguided |
|---|---:|---:|---:|---:|
| ImageNette | 42.12 ± 0.93 | 47.79 ± 0.26 | **55.79 ± 0.23** | **+16.8%** |
| ImageWoof | 22.21 ± 0.01 | 26.63 ± 0.55 | **30.30 ± 0.34** | **+13.7%** |

CAGS 的收益不局限于 ImageNet-100 的类别划分。ImageNette（易）和 ImageWoof（难，细粒度犬种）上均获得强劲增益，CAGS 相对 Random Herding 的总优势分别达到 +32.5% 和 +36.4%。

### 6.5 100 类模块消融（IPC=10）

| 配置 | Top-1 (%) | Δ vs unguided |
|---|---:|---:|
| Unguided | 19.66 ± 0.18 | — |
| Fixed `lambda=0.10` | 19.29 ± 0.10 | −0.37 |
| CAGS v2 | **21.11 ± 0.50** | **+1.45** |
| IAST+CAGS v2 (IPC=10，等同 CAGS) | 21.11 ± 0.50 | +1.45 |
| Linear TAGS+CAGS v2 | 20.31 ± 0.44 | +0.65 |

IAST 在 IPC=10 上因 range_scale=1.0 而完全透明。TAGS 不提供额外增益，确认类别轴是主要变异轴。

### 6.6 FID

| 数据集 | IPC | Unguided | CAGS v2 | 变化（越低越好） |
|---|---:|---:|---:|---:|
| ImageNet-100（100 类） | 10 | 88.66 | **85.26** | −3.40 |
| ImageNet-100（100 类） | 50 | 71.06 | **59.28** | −11.78 |
| ImageNette | 10 | **105.21** | 108.59 | +3.38 |
| ImageWoof | 10 | **86.27** | 94.70 | +8.42 |

FID 在生成图像上计算，不随评估协议变化，因此仍为有效值。100 类任务中 CAGS 同时改善 FID 和准确率。ImageNette/Woof 出现 FID 变差但准确率提高的现象，说明 CAGS 优化的是训练数据效用，而非单纯的分布相似度。

## 7. 尚未完成或需进一步验证的工作

- 外部 DD 方法（D4M、SRe²L、MTT、DMGD、CoDA、IDC-1、MinMaxDiff 等）的统一协议比较表尚未完成；
- 统计显著性检验、95% 置信区间和效应量尚未计算；
- 类别级复杂度—lambda—准确率增益相关性分析尚未进行；
- TAGS-only 在新协议下的完整评估尚未完成；
- IAST 缩放函数的条件化设计（根据类别数调整）值得探索。

## 8. 对实验结果的总体分析

### 8.1 核心证据链

实验形成了一条清晰的逐级证据链：

1. **CAGS 全面超过 unguided 和 fixed λ**：10 类 IPC=10 CAGS 50.13% > Fixed 48.47% > Unguided 45.40%；100 类 IPC=10 CAGS 21.11% > Unguided 19.66% > Fixed 19.29%；
2. **CAGS 增益随 IPC 单调递增**：10 类 +2.2% → +10.4% → +15.5%；100 类 +1.6% → +7.4% → +19.4%；
3. **IAST 在 100 类 IPC=1 上发挥关键保护**：CAGS alone 仅 +1.6%，IAST+CAGS 达到 +11.2%；
4. **IAST 在 10 类 IPC=1 上有害**：−13.0%，揭示了适用边界；
5. **TAGS 不提供额外增益**：TAGS+CAGS 20.31% < CAGS alone 21.11%，确认类别轴是主要变异轴；
6. **CAGS 跨架构有效**：ConvNet-6 +7.4%，ResNet-18 +13.8%，ResNetAP-10 +15.2%；
7. **CAGS 跨数据集有效**：ImageNette +16.8%，ImageWoof +13.7%。

### 8.2 CAGS 的适用性

修正协议后，CAGS 在所有测试设置中均优于 unguided，消除了旧协议下"10 类 IPC=10 CAGS 不如 fixed λ"的弱点。唯一需要 IAST 辅助的场景是 100 类 IPC=1，此时 CAGS alone 仅 +1.6% 而 IAST+CAGS 达到 +11.2%。CAGS 的增益随 IPC 和类别数增加而增大，与"复杂度变异越大，逐类适配价值越高"的假设一致。

### 8.3 IAST 的适用边界

IAST 的对数缩放在 100 类 IPC=1 有效、10 类 IPC=1 有害，是一个重要的科学发现而非方法缺陷。机制解释：100 类复杂度变异大（std=0.082），缩放后范围 [0, 0.017] 仍保留足够的逐类差异；10 类复杂度变异有限，缩放后范围 [0, 0.023] 的逐类差异低于噪声水平。未来工作应将缩放函数条件化于类别数。

### 8.4 TAGS 的定位

TAGS 在修正协议下不提供额外增益（20.31% < 21.11%），但这不是负面结果。它确认了固定引导的主要缺陷来源是逐类引导失配，而非引导的时间分布。论文应将 TAGS 定位为消融实验中的科学发现，而非核心贡献模块。

### 8.5 Random Herding 与 unguided 的交叉

Random Herding 在 IPC=50 上超过 unguided（10 类：64.73 vs 58.93；100 类：35.21 vs 31.52），因为高预算时真实图像的多样性优势显现。但 CAGS 在所有设置中均超过 Random Herding，证明自适应引导使合成数据即使面对真实图像的多样性优势仍具竞争力。

### 8.6 FID 与准确率背离

CAGS 在 100 类上同时改善 FID 和准确率，但在 ImageNette/Woof 上 FID 变差而准确率提高。这说明 CAGS 优化的是训练数据效用，而非单纯的分布相似度。该定位符合数据集蒸馏目标。

### 8.7 协议修正的影响

协议修正（Lighting + 归一化顺序）使绝对准确率大幅提升（100 类 IPC=10 unguided：12.27% → 19.66%），与 MGD³ 发表值量级一致。更重要的是，修正后 CAGS 在 10 类 IPC=10 上超过 fixed λ，消除了旧协议下的弱点，论文主线更强。

## 9. 建议的论文主线

1. MGD³ 证明冻结扩散模型可用于训练自由数据集蒸馏，但固定 CFG 假设过强；
2. 10 类上适度固定引导有效（+6.8%），但 CAGS 超过最佳固定 λ（+10.4% vs +6.8%），证明逐类适配即使在低异质性下也有价值；
3. 类别增至 100 后，固定引导出现负迁移（−1.9%），暴露统一参数的局限；
4. CAGS 根据类别复杂度分配引导，在 100 类上从负增益转为 +7.4%，并在多个数据集和 IPC 上持续超过 unguided 和 fixed λ；
5. IAST 在 100 类 IPC=1 上发挥关键保护（+11.2%），但在 10 类 IPC=1 上有害（−13.0%），揭示了适用边界；
6. TAGS 不提供额外增益，确认类别轴是主要变异轴；
7. 跨架构验证证明 CAGS 改进不与特定架构耦合，更深网络获益更大；
8. FID 与准确率并非总是一致，蒸馏方法应直接优化下游效用。

## 10. 下一步优先级

1. 按统一协议整理最新 DD 方法比较表（IDC-1、MinMaxDiff 等）；
2. 对主要方法执行配对显著性检验并报告 95% 置信区间与效应量；
3. 增加类别级复杂度—lambda—准确率增益相关性分析；
4. 探索 IAST 缩放函数的条件化设计（根据类别数调整）；
5. 补充新协议下的 TAGS-only 评估。

## 11. 当前审阅结论

全部实验已在修正后的 MGD³ 匹配协议下完成。CAGS 识别并缓解了固定 CFG 在类别异质性条件下的失效，是核心贡献，在所有数据集 × IPC × 架构组合中均优于 unguided，并超过最佳固定 λ。IAST 在 100 类 IPC=1 上发挥关键保护作用（+11.2%），但在 10 类 IPC=1 上有害，揭示了重要适用边界。TAGS 不提供额外增益，确认类别轴是主要变异轴。跨架构验证证明改进不与 ConvNet-6 耦合，ResNet-18 和 ResNetAP-10 上的增益更大。Random Herding 在 IPC=50 上超过 unguided 但被 CAGS 超越。FID 与准确率的背离进一步支持"蒸馏方法应以下游效用为核心指标"的定位。剩余工作主要是外部方法比较表、统计检验和机制分析，当前思路已具备形成完整投稿的实验基础。
