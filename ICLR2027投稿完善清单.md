# AGS-DD 投稿 ICLR 2027 完善清单

## 一、当前状态总览

| 维度 | 完成度 | 说明 |
|------|--------|------|
| 核心代码 | ✅ 90% | CAGS/IAST/TAGS 三模块 + 采样管道 + 评估代码齐全 |
| TAGS 方向 bug | ✅ 已修复 | 2026-08-31 修复并推送，但旧实验结果全部作废 |
| 实验数据（生成） | ⚠️ 50% | 仅 ImageNette/ImageWoof，且 TAGS 相关结果因 bug 作废 |
| 实验数据（评估） | ❌ 0% | 没有任何 accuracy 结果，论文核心表格全缺 |
| 竞品对比 | ❌ 0% | 未实现 CoDA/DMGD/ManifoldGD 等竞品对比 |
| 论文撰写 | ❌ 0% | 无 .tex/.bib 文件 |
| 文档 | ⚠️ 60% | 有 README 和新手指南，但 requirements.txt 不完整 |

---

## 二、紧急：实验数据全部需要重新生成

### 2.1 TAGS 方向 bug 导致的作废范围

修复前 TAGS 在低噪声阶段施加引导（与设计相反），以下实验结果**全部作废**：

| 实验 | nette | woof | 原因 |
|------|-------|------|------|
| `ags_full_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `ags_full_ipc50` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `cags_tags_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `iast_tags_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `tags_only_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `tags_linear_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `tags_exp_ipc10` | ❌ 作废 | ❌ 作废 | use_tags=True |
| `baseline_ipc10` | ✅ 保留 | ✅ 保留 | use_tags=False |
| `baseline_ipc50` | ✅ 保留 | ✅ 保留 | use_tags=False |
| `cags_only_ipc10` | ✅ 保留 | ✅ 保留 | use_tags=False |
| `iast_only_ipc10` | ✅ 保留 | ✅ 保留 | use_tags=False |
| `cags_iast_ipc10` | ✅ 保留 | ✅ 保留 | use_tags=False |

**需重新生成的实验**：14 个（nette 7 个 + woof 7 个）

### 2.2 IAST lambda 参数不一致

存在严重的参数不一致问题：

| 位置 | lambda 值 | 说明 |
|------|-----------|------|
| `adaptive_stop.py` 默认值 | **0.316** | sqrt scaling 的正确值 |
| `sample_ags.py` 默认值 | 0.1 | 旧值（线性 scaling） |
| `configs/default.yaml` | 0.1 | 旧值 |
| `run_all_experiments.py` | 0.1 | 旧值 |
| `regenerate_ipc50.py` | 0.316 | 修正值 |

所有已生成实验使用的是 `lambda=0.1`，但 git log 显示 `ffc4bdf` commit 已将 IAST 改为 sqrt scaling 并将 lambda 调整为 0.316。**`run_all_experiments.py` 未同步更新**，导致实际跑的实验用的是错误的旧 lambda。

**需做**：统一 lambda=0.316，重新生成所有 IAST 启用的实验。

### 2.3 重跑清单

以统一 `lambda=0.316` + 修复后 TAGS 重新生成：

```
# 必须重跑（TAGS 受影响或 lambda 不对）
nette: ags_full_ipc10, ags_full_ipc50, cags_tags, iast_tags, tags_only, tags_linear, tags_exp
woof:  ags_full_ipc10, ags_full_ipc50, cags_tags, iast_tags, tags_only, tags_linear, tags_exp

# 建议重跑（lambda 0.1→0.316 影响 IAST）
nette: cags_iast_ipc10, iast_only_ipc10
woof:  cags_iast_ipc10, iast_only_ipc10

# 可保留（无 TAGS 无 IAST）
nette: baseline_ipc10, baseline_ipc50, cags_only_ipc10
woof:  baseline_ipc10, baseline_ipc50, cags_only_ipc10
```

**预计时间**：单卡约 6-8 小时，双卡约 3-4 小时。

---

## 三、缺失实验（论文必需但尚未跑）

### 3.1 评估准确率（最关键缺口）

**现状**：仓库有 8800 张生成图片，但**零条 accuracy 结果**。论文的主表是 accuracy，没有这个等于没有论文。

需运行的评估：

| 评估类型 | 命令入口 | 架构 | 预计时间 |
|---------|---------|------|---------|
| 主实验 | `evaluation.train_eval` | ConvNet-7 | 每配置 ~15min |
| 跨架构 | `evaluation.cross_arch` | ConvNet-7, ResNet-18, ViT-Tiny, Swin-Tiny, DeiT-Tiny | 每配置 ~1.5h |

**计算量**：
- 主实验：2 数据集 × 2 IPC × 3 seeds × 2 方法(ags+baseline) = 24 次评估
- 消融：2 数据集 × 8 配置 × 1 seed = 16 次评估
- 跨架构：2 数据集 × 2 IPC × 5 架构 × 3 seeds = 60 次评估
- **总计约 100 次评估，约 30-40 小时单卡**

### 3.2 缺失的数据集实验

| 数据集 | 代码支持 | 实验结果 | ICLR 必要性 |
|--------|---------|---------|-------------|
| ImageNette (10类) | ✅ | ⚠️ 需重跑 | 调试用，论文中作为快速验证 |
| ImageWoof (10类) | ✅ | ⚠️ 需重跑 | 细粒度验证 |
| ImageNet-100 (100类) | ✅ | ❌ | **必需**，中等规模验证 |
| ImageNet-1K (1000类) | ✅ | ❌ | **必需**，大规模验证是核心卖点 |
| CIFAR-10/100 | ❌ | ❌ | 推荐，DD 领域标准 benchmark |

**ImageNet-1K 实验估算**：
- 生成：1000 类 × 50 IPC × 3 datasets ÷ 100 类/phase × 10 phases ≈ 单卡 20+ 小时
- 评估：需大量训练时间
- **建议至少使用 2× 24GB GPU 并行**

### 3.3 缺失的 IPC 设置

| IPC | ImageNette | ImageWoof | ImageNet-100 | ImageNet-1K |
|-----|-----------|-----------|-------------|-------------|
| 1 | ❌ | ❌ | ❌ | ❌ |
| 10 | ⚠️ 需重跑 | ⚠️ 需重跑 | ❌ | ❌ |
| 50 | ⚠️ 需重跑 | ⚠️ 需重跑 | ❌ | ❌ |
| 100 | ❌ | ❌ | ❌ | ❌ |
| 200 | ❌ | ❌ | ❌ | ❌ |

DD 论文标准做法：至少报告 IPC=1, 10, 50 三个设置。

### 3.4 FID 指标未实现

`evaluation/metrics.py` 文档注释中提到 FID，但代码未实现。论文需要 FID 来证明生成质量。

**需做**：实现 FID 计算（使用 `pytorch-fid` 或 `clean-fid`），对每个生成的数据集计算与真实数据的 FID。

### 3.5 竞品对比缺失

以下竞品必须对比（均为 2025-2026 年发表/预印本）：

| 竞品 | 类型 | 必要性 | 对比方式 |
|------|------|--------|---------|
| **MGD³** (ICML 2025 Oral) | 直接基线 | 必需 | 代码已有 `--baseline` 模式 |
| **CoDA** (2025) | Training-free T2I DD | 必需 | 需获取/复现代码 |
| **DMGD** (2026) | Training-free 语义分布匹配 | 必需 | 需获取/复现代码 |
| **ManifoldGD** (2026) | Training-free 层次流形引导 | 必需 | 需获取/复现代码 |
| **Adaptive Sampling** (AAAI 2026) | 自适应采样 | **关键威胁** | 需明确区分 + 实验对比 |
| **Minimax Diffusion** (CVPR 2024) | 需训练的扩散 DD | 推荐 | 标准基线 |
| **D4M** (CVPR 2024) | 解耦扩散 DD | 推荐 | 标准基线 |

---

## 四、代码层面待完善

### 4.1 已知 bug 和问题

| 问题 | 严重性 | 状态 | 说明 |
|------|--------|------|------|
| TAGS 方向反转 | 严重 | ✅ 已修复 | 2026-08-31 修复推送 |
| IAST lambda 不一致 | 严重 | ❌ 未修复 | 默认值 0.316 vs 实际用 0.1 |
| requirements.txt 缺失依赖 | 中 | ❌ 未修复 | 缺 timm, efficientnet_pytorch, scikit-image, pandas |
| FID 未实现 | 中 | ❌ | metrics.py 有注释无代码 |
| 聚类缓存被 gitignore | 低 | ❌ | 每次重跑需重新聚类 ~1.5min |

### 4.2 IAST lambda 修复建议

```python
# sample_ags.py 第 115 行
parser.add_argument("--iast-lambda", type=float, default=0.316,  # 从 0.1 改为 0.316

# run_all_experiments.py 第 283 行
iast_lambda=0.316,  # 从 0.1 改为 0.316

# configs/default.yaml 第 35 行
lambda: 0.316  # 从 0.1 改为 0.316

# scripts/run_imagenette.sh 第 33 行
--iast-lambda 0.316  # 从 0.1 改为 0.316

# scripts/run_imagewoof.sh 第 33 行
--iast-lambda 0.316  # 从 0.08 改为 0.316
```

### 4.3 建议新增功能

| 功能 | 优先级 | 说明 |
|------|--------|------|
| FID 计算 | 高 | 用 clean-fid 库，加到 `evaluation/metrics.py` |
| 聚类缓存保存/加载 | 中 | 避免每次重跑聚类，省 ~1.5min/数据集 |
| CIFAR-10/100 支持 | 中 | DD 领域标准 benchmark，扩大适用范围 |
| 实验结果自动汇总表格 | 中 | 将 JSON 结果自动生成 LaTeX 表格 |
| 多 GPU DataParallel | 低 | ImageNet-1K 实验加速 |

---

## 五、论文撰写（从零开始）

### 5.1 论文结构（ICLR 标准 9 页正文）

| 章节 | 页数 | 内容 | 当前状态 |
|------|------|------|---------|
| Abstract | 0.3 | 问题、方法、关键结果 | ❌ |
| Introduction | 1.0 | 动机、贡献、论文结构 | ❌ |
| Related Work | 1.0 | DD、扩散引导、训练自由方法 | ❌ |
| Method | 2.5 | CAGS + IAST + TAGS 三模块 + 集成 | ❌ |
| Experiments | 3.0 | 主实验 + 消融 + 跨架构 + FID + 可视化 | ❌ |
| Conclusion | 0.2 | 总结 + 局限 + 未来工作 | ❌ |
| References | - | ≥30 篇引用 | ❌ |

### 5.2 论文核心卖点需明确

基于竞品分析，AGS-DD 的差异化定位：

1. **vs MGD³**：MGD³ 用固定参数，AGS-DD 自适应（per-class, per-IPC, per-timestep）
2. **vs Adaptive Sampling (AAAI 2026)**：他们做自适应**采样选择**（选哪些样本），我们做自适应**引导调度**（如何引导生成）
3. **vs CoDA/DMGD/ManifoldGD**：它们是独立的 training-free 方法，AGS-DD 是 MGD³ 的**即插即用增强**
4. **vs 通用 guidance scheduling (SIGGRAPH 2025)**：通用方法不针对 DD，不考虑 per-class 复杂度和 IPC 约束

### 5.3 论文必需的图表

| 图表 | 类型 | 内容 | 数据来源 |
|------|------|------|---------|
| Table 1 | 主实验 | 4 数据集 × 3 IPC × 5 架构的 Top-1 accuracy | 需跑评估 |
| Table 2 | 消融 | 8 种配置 × 2 数据集的 accuracy | 需跑评估 |
| Table 3 | 竞品对比 | AGS-DD vs MGD³/CoDA/DMGD/ManifoldGD | 需跑竞品 |
| Table 4 | FID | 生成质量对比 | 需实现 FID |
| Figure 1 | 方法概览 | CAGS/IAST/TAGS 流程图 | 需绘制 |
| Figure 2 | TAGS 调度曲线 | 不同 schedule 类型的权重曲线 | 可从代码生成 |
| Figure 3 | t-SNE 可视化 | 真实 vs 蒸馏数据特征分布 | `tsne_plots.py` 可生成 |
| Figure 4 | 生成样本展示 | 各方法生成的图片对比 | 从 results/ 选取 |
| Figure 5 | 复杂度分析 | 类复杂度 vs guidance strength | 从 CAGS 输出提取 |

### 5.4 必需的参考文献（初步，需 paper_search 验证）

至少需要引用以下方向的工作：

- **MGD³** (ICML 2025) — 直接基线
- **Minimax Diffusion** (CVPR 2024), **D4M** (CVPR 2024) — 扩散 DD 基线
- **CoDA**, **DMGD**, **ManifoldGD** (2025-2026) — Training-free 竞品
- **Adaptive Sampling Framework** (AAAI 2026) — 概念最接近的竞品
- **Annealing Guidance Scale** (SIGGRAPH 2025) — guidance scheduling 先行工作
- **DATM** (ICLR 2024) — 难度对齐轨迹匹配（IAST 灵感来源）
- **DiT** (ICCV 2023) — 扩散模型架构
- 经典 DD 方法：DC, DSA, DM, MTT, FRePo, SRe2L, TESLA 等

---

## 六、时间规划建议

以 ICLR 2027 为目标（假设截稿 2026 年 9-10 月）：

| 阶段 | 内容 | 预计时间 | 优先级 |
|------|------|---------|--------|
| **第 1 周** | 修复 IAST lambda，统一参数，补全 requirements.txt | 1 天 | 紧急 |
| **第 1-2 周** | 重跑所有 TAGS 相关生成实验（nette+woof） | 3-4 天（双卡） | 紧急 |
| **第 2-3 周** | 跑全部评估实验（accuracy + 跨架构） | 2-3 天 | 紧急 |
| **第 3-4 周** | ImageNet-100 实验（生成+评估） | 3-4 天 | 高 |
| **第 4-5 周** | ImageNet-1K 实验（生成+评估） | 5-7 天 | 高 |
| **第 3-5 周** | 实现 FID，获取/复现竞品代码 | 并行 | 高 |
| **第 5-6 周** | 竞品对比实验 | 3-5 天 | 高 |
| **第 6-8 周** | 论文撰写（Method + Experiments 优先） | 2 周 | 高 |
| **第 8-9 周** | 补充实验、绘制图表、润色 | 1 周 | 中 |
| **第 9-10 周** | 全文审校、附录、Rebuttal 预判 | 1 周 | 中 |

**总预计**：6-8 周高强度工作（假设 2× 24GB GPU 持续可用）。

---

## 七、ICLR 审稿风险预判

### 7.1 高风险项

| 风险 | 严重性 | 应对策略 |
|------|--------|---------|
| **创新性不足** | 高 | 三模块都是"在已有方法上做自适应"，需强调三者的协同效应和理论分析 |
| **与 AAAI 2026 Adaptive Sampling 重叠** | 高 | 明确区分：他们做采样选择，我们做引导调度；实验中直接对比 |
| **仅在 ImageNet 子集上实验** | 高 | 必须 ImageNet-1K 全量 + CIFAR 标准基准 |
| **无理论分析** | 中 | 补充 TAGS 调度与 diffusion 采样轨迹的理论联系 |
| **竞品不全面** | 高 | 必须 CoDA/DMGD/ManifoldGD 全对比 |
| **TAGS bug 历史** | 中 | 确保提交时所有结果用修复后代码生成 |

### 7.2 审稿人可能提问

1. "CAGS 的复杂度评分依赖 K-means，如何保证聚类质量？"
2. "IAST 的 sqrt(IPC) scaling 公式是如何推导的？有理论依据吗？"
3. "TAGS 的 cosine schedule 相比 linear/exp 的优势在哪？有消融吗？"
4. "三个模块是否正交？去掉一个对另外两个的影响？"
5. "在 ImageNet-1K 上的计算成本是多少？与训练型方法（如 Minimax）的 GPU-hours 对比？"
6. "为什么不用更新的扩散模型（如 SDXL、Flux）？仅用 DiT-XL/2 是否限制了方法泛化性？"
7. "方法是否依赖 VAE 特征质量？换用其他特征提取器（如 CLIP/DINO）效果如何？"

---

## 八、优先级排序（行动清单）

### P0 — 不做无法投稿

- [ ] 修复 IAST lambda 不一致（统一为 0.316）
- [ ] 补全 requirements.txt
- [ ] 重新生成所有 TAGS 相关实验数据（nette + woof）
- [ ] 运行全部评估实验获取 accuracy 数据
- [ ] ImageNet-100 实验（生成 + 评估）
- [ ] ImageNet-1K 实验（生成 + 评估）
- [ ] 撰写论文主体

### P1 — 不做审稿必被质疑

- [ ] 竞品对比（CoDA, DMGD, ManifoldGD）
- [ ] FID 指标实现 + 计算
- [ ] 跨架构评估
- [ ] 消融实验完整版（含 8 种配置 × 多 IPC）
- [ ] TAGS schedule 类型对比消融

### P2 — 锦上添花

- [ ] CIFAR-10/100 支持
- [ ] t-SNE 可视化
- [ ] 生成样本展示图
- [ ] 计算成本分析表
- [ ] 理论分析章节
- [ ] 不同扩散模型（SDXL 等）的泛化验证
